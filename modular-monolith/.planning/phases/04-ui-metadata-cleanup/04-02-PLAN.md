---
phase: 04-ui-metadata-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [
  "src/modules/auto-action/AutoActionService.ts",
  "src/modules/auto-action/internal/service/AutoActionServiceImpl.ts",
  "src/graphql/schema/auto-action/index.ts",
  "src/graphql/resolvers/autoAction.ts",
  ".planning/phases/04-ui-metadata-cleanup/VALIDATION.md"
]
autonomous: true
requirements: ["REQ-3"]

must_haves:
  truths:
    - "getBehaviorSettingsCatalog returns the correct behavior metadata."
    - "GraphQL query behaviorSettingsCatalog is functional."
    - "Knowledge graph is updated."
---

<objective>
Expose CWB behavior metadata to the UI and finalize documentation.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement getBehaviorSettingsCatalog</name>
  <files>src/modules/auto-action/AutoActionService.ts, src/modules/auto-action/internal/service/AutoActionServiceImpl.ts</files>
  <action>
    - Add `getBehaviorSettingsCatalog(projectId: string)` to the interface.
    - Implement it in `AutoActionServiceImpl` to return static metadata for:
      - `BLOCKER_RESOLUTION`, `PARENT_DELETE_GUARD`, `PRIORITY_CASCADE`, `TEAM_CASCADE`, `BLOCKER_SAFETY_GUARD`, `MEMBER_ASSIGNMENT_GUARD`, `CASCADE_DELETE`, `AUTO_NOTIFY`.
  </action>
  <verify>
    <automated>bun x tsc --noEmit</automated>
  </verify>
  <done>Metadata service method implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Expose via GraphQL</name>
  <files>src/graphql/schema/auto-action/index.ts, src/graphql/resolvers/autoAction.ts</files>
  <action>
    - Define `BehaviorSetting` and `BehaviorSettingsCatalog` types in GraphQL schema.
    - Add `behaviorSettingsCatalog(projectId: ID!)` Query.
    - Implement resolver in `autoAction.ts`.
  </action>
  <verify>
    <automated>grep "behaviorSettingsCatalog" src/graphql/resolvers/autoAction.ts</automated>
  </verify>
  <done>GraphQL layer updated.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize Phase 4</name>
  <files>.planning/phases/04-ui-metadata-cleanup/VALIDATION.md</files>
  <action>
    - Update VALIDATION.md with evidence.
    - Run `graphify update .`.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-ui-metadata-cleanup/VALIDATION.md && graphify update .</automated>
  </verify>
  <done>Phase 4 completed and verified.</done>
</task>
</tasks>
