---
phase: 04-ui-metadata-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/modules/auto-action/internal/engines/",
  "src/modules/auto-action/internal/execution/",
  "src/modules/auto-action/scopes/task/",
  "src/modules/auto-action/AutoActionService.ts",
  "src/modules/auto-action/internal/service/AutoActionServiceImpl.ts",
  "src/modules/auto-action/index.ts",
  "src/modules/auto-action/types.ts",
  "src/modules/auto-action/internal/queries/AutoActionQueries.ts"
]
autonomous: true
requirements: ["REQ-1.4"]

must_haves:
  truths:
    - "Legacy AST engines, executors, and task scopes are deleted."
    - "Dangling imports in AutoActionService and TaskEventConsumer are removed."
    - "The project compiles successfully with tsc."
---

<objective>
Delete legacy AST-based code bloat and clean up the AutoAction module to be CWB-exclusive.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Delete Legacy AST Directories</name>
  <files>src/modules/auto-action/internal/engines, src/modules/auto-action/internal/execution, src/modules/auto-action/scopes/task</files>
  <action>
    - Delete `src/modules/auto-action/internal/engines/` and all contents.
    - Delete `src/modules/auto-action/internal/execution/` and all contents.
    - Delete `src/modules/auto-action/scopes/task/` and all contents.
  </action>
  <verify>
    <automated>test ! -d src/modules/auto-action/internal/engines && test ! -d src/modules/auto-action/internal/execution</automated>
  </verify>
  <done>Legacy AST directories removed.</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup Service and Imports</name>
  <files>src/modules/auto-action/AutoActionService.ts, src/modules/auto-action/internal/service/AutoActionServiceImpl.ts, src/modules/auto-action/index.ts, src/modules/auto-action/types.ts</files>
  <action>
    - Remove imports of `ActionEngine`, `ConditionEngine`, `Executor`, and `taskScope` from all files.
    - Remove references to `handleTaskEvents` (legacy version) from `AutoActionService` interface and implementation.
    - Remove legacy AST validation logic from `createAutoAction` and `updateAutoAction`.
    - Strip legacy types from `src/modules/auto-action/types.ts`.
  </action>
  <verify>
    <automated>bun x tsc --noEmit</automated>
  </verify>
  <done>AutoAction module cleaned of legacy references; project compiles.</done>
</task>
</tasks>
