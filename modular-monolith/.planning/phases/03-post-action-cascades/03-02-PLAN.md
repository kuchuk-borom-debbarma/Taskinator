---
phase: 03-post-action-cascades
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/modules/auto-action/internal/listeners/AutoActionTaskEventConsumer.ts, src/tests/verify-phase-3.ts, .planning/phases/03-post-action-cascades/VALIDATION.md]
autonomous: true
requirements: [1.3, 3]
must_haves:
  truths:
    - "AutoActionTaskEventConsumer uses CascadeService for side-effects"
    - "matchesCriteria correctly filters rules based on task fields"
    - "Integration tests verify all 4 cascade types (BLOCKER, PRIORITY, TEAM, DELETE)"
  artifacts:
    - path: "src/tests/verify-phase-3.ts"
      provides: "Phase 3 functional verification"
---

<objective>
Refactor the event consumer to trigger CWB cascades and verify the entire reactive pipeline with integration tests.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Refactor AutoActionTaskEventConsumer</name>
  <files>src/modules/auto-action/internal/listeners/AutoActionTaskEventConsumer.ts</files>
  <action>
    - Inject `cascadeService` into the consumer.
    - Implement `matchesCriteria(task, rule)` helper to handle EQUALS, NOT_EQUALS, GREATER_THAN, LESS_THAN operators.
    - Update `handleTaskEvents` to:
      1. Fetch active behavior rules for the project from `behavior_rule` table.
      2. Filter rules using `matchesCriteria`.
      3. Switch on `rule.behavior_type` and call corresponding `cascadeService` methods.
      4. Ensure `actorId` and `traceId` are passed from the domain event data.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Consumer refactored to use new CWB rule engine for cascades.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Integration Tests</name>
  <files>src/tests/verify-phase-3.ts</files>
  <action>
    Create src/tests/verify-phase-3.ts to test:
    - BLOCKER_RESOLUTION: Subtask DONE -> Parent READY.
    - PRIORITY_CASCADE: Parent High -> Subtask High.
    - TEAM_CASCADE: Parent Team A -> Subtask Team A.
    - CASCADE_DELETE: Parent Deleted -> Subtasks Deleted.
    Ensure tests wait for async Kafka/EventBus processing if needed, or mock the trigger to call consumer directly for speed while maintaining E2E-like logic.
  </action>
  <verify>
    <automated>bun run src/tests/verify-phase-3.ts</automated>
  </verify>
  <done>Integration tests pass for all reactive scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize Documentation and Graph</name>
  <files>.planning/phases/03-post-action-cascades/VALIDATION.md</files>
  <action>
    - Update VALIDATION.md for Phase 3 with test results.
    - Run `graphify update .` to refresh the knowledge graph.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-post-action-cascades/VALIDATION.md && graphify update .</automated>
  </verify>
  <done>Phase 3 finalized and graph updated.</done>
</task>
</tasks>
