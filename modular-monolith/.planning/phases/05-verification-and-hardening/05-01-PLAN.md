---
phase: 05-verification-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/task/internal/CascadeService.ts
  - src/tests/e2e/CwbIntegration.test.ts
  - src/tests/performance/ReachabilityProfiling.ts
autonomous: true
requirements:
  - "Add E2E tests for all Guard scenarios"
  - "Add E2E tests for all Cascade scenarios"
  - "Performance profiling of Kysely relationship queries"
  - "Fix the 'blocks' vs 'BLOCKER' label mismatch"

must_haves:
  truths:
    - "CwbIntegration tests cover all 3 Guard types (PARENT_DELETE, BLOCKER_SAFETY, MEMBER_ASSIGNMENT)"
    - "CwbIntegration tests cover all 4 Cascade types (BLOCKER_RESOLUTION, PRIORITY, TEAM, DELETE)"
    - "CascadeService uses the standardized 'blocks' label for blocker resolution"
    - "System performance for reachability queries is documented via profiling script"
  artifacts:
    - path: "src/modules/task/internal/CascadeService.ts"
      provides: "Standardized blocker resolution logic"
    - path: "src/tests/e2e/CwbIntegration.test.ts"
      provides: "Full integration coverage for CWB"
    - path: "src/tests/performance/ReachabilityProfiling.ts"
      provides: "Benchmarking for graph-based queries"
  key_links:
    - from: "src/modules/task/internal/CascadeService.ts"
      to: "task_link table"
      via: "standardized 'blocks' label"
---

<objective>
Hardening the CWB implementation by standardizing relationship labels, implementing a comprehensive E2E test suite for all Guard and Cascade behaviors (including complex edge cases), and profiling the performance of graph-based reachability queries to ensure scalability.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/phase-5/RESEARCH.md
@src/modules/task/internal/CascadeService.ts
@src/modules/task/internal/GuardQueries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize labels and Performance Profiling</name>
  <files>src/modules/task/internal/CascadeService.ts, src/tests/performance/ReachabilityProfiling.ts</files>
  <action>
    - Standardize Blocker Label: Update `CascadeService.ts` to use 'blocks' (lowercase) instead of 'BLOCKER' in `resolveBlockers` queries to align with `GuardQueries.ts` and existing E2E tests.
    - Implement Performance Profiling: Create `src/tests/performance/ReachabilityProfiling.ts` using Kysely and Node `performance.now()`.
    - Profiling Goal: Measure execution time for `task_reachability` descendant lookups and `CascadeService.resolveBlockers` subqueries on a graph with 100+ tasks and 10+ levels of depth.
  </action>
  <verify>
    <automated>! grep -i "'BLOCKER'" src/modules/task/internal/CascadeService.ts && bun run src/tests/performance/ReachabilityProfiling.ts</automated>
  </verify>
  <done>Labels standardized to 'blocks' and performance baseline established.</done>
</task>

<task type="auto">
  <name>Task 2: Implement E2E Integration Tests (Guards & Edge Cases)</name>
  <files>src/tests/e2e/CwbIntegration.test.ts</files>
  <action>
    - Create `src/tests/e2e/CwbIntegration.test.ts` using the bootstrap pattern from `task.create.test.ts`.
    - Implement tests for:
      - `PARENT_DELETE_GUARD`: Verify that deleting a parent task with active subtasks is blocked.
      - `BLOCKER_SAFETY_GUARD`: Verify that updating a task status to 'IN_PROGRESS' is blocked if it has incomplete blockers.
      - `MEMBER_ASSIGNMENT_GUARD`: Verify that assigning a member to a task with no team is blocked.
      - **Multi-Violation**: Trigger both a subtask guard and a blocker guard in one request; verify the error response.
      - **Circularity**: Verify that the system handles circular links (if allowed) or prevents them from breaking guards.
    - Ensure `behavior_rule` records are seeded for the specific project in each test.
  </action>
  <verify>
    <automated>bun test src/tests/e2e/CwbIntegration.test.ts --grep "Guard"</automated>
  </verify>
  <done>All three Pre-Action Guards and identified edge cases verified via E2E GraphQL requests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement E2E Integration Tests (Cascades) and Finalize</name>
  <files>src/tests/e2e/CwbIntegration.test.ts, .planning/phases/05-verification-and-hardening/VALIDATION.md</files>
  <action>
    - Extend `CwbIntegration.test.ts` to cover reactive cascades.
    - Implement tests for:
      - `BLOCKER_RESOLUTION`: Mark subtask as DONE -> Poll and verify parent transitions to READY.
      - `PRIORITY_CASCADE`: Update parent priority -> Poll and verify all descendants (recursive) update.
      - `TEAM_CASCADE`: Update parent team -> Poll and verify all descendants (recursive) update.
      - `CASCADE_DELETE`: Delete parent -> Poll and verify all descendants are removed from DB.
    - Update `VALIDATION.md` with evidence from all tasks.
    - Run `graphify update .` to keep the knowledge graph current.
  </action>
  <verify>
    <automated>bun test src/tests/e2e/CwbIntegration.test.ts --grep "Cascade" && graphify update .</automated>
  </verify>
  <done>All four Post-Action Cascades verified, validation report updated, and knowledge graph synced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GraphQL -> GuardService | Untrusted user input (status, team assignment) must be validated against behavior rules |
| Kafka -> CascadeService | Asynchronous propagation of changes must be idempotent to prevent loops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Behavior Rules | mitigate | Ensure rules are project-scoped in queries (already implemented in Phase 1-4) |
| T-05-02 | Denial of Service | Graph Queries | mitigate | Performance profiling in Task 1 identifies slow queries for optimization |
| T-05-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Overall phase verification is achieved by running the full CwbIntegration.test.ts suite and reviewing the ReachabilityProfiling.ts output.
</verification>

<success_criteria>
- Labels are consistent across Guards and Cascades.
- 100% of defined Guards and Cascades have at least one E2E happy-path and one failure-path (where applicable) test case.
- Performance profiling reveals no queries exceeding 50ms for standard graph sizes (100 nodes).
</success_criteria>

<output>
Create `.planning/phases/05-verification-and-hardening/05-01-SUMMARY.md` when done
</output>
