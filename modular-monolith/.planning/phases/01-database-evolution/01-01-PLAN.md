---
phase: "01-database-evolution"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "database/migration_cwb_init.sql",
  "src/database/tables/BehaviorRule.ts",
  "src/database/index.ts",
  "src/tests/verify-phase-1.ts",
  ".planning/phases/01-database-evolution/VALIDATION.md"
]
autonomous: true
requirements: ["REQ-1.1", "REQ-1.4"]

must_haves:
  truths:
    - "The behavior_rule table exists with the specified columns."
    - "The auto_action table is empty (truncated)."
    - "Kysely types for behavior_rule are integrated into the Database interface."
    - "Knowledge graph is synchronized with the new schema changes."
  artifacts:
    - path: "database/migration_cwb_init.sql"
      provides: "SQL schema for the new behavior_rule table and legacy wipe"
    - path: "src/database/tables/BehaviorRule.ts"
      provides: "Kysely type definitions for behavior_rule"
    - path: ".planning/phases/01-database-evolution/VALIDATION.md"
      provides: "Requirement verification records"
  key_links:
    - from: "src/database/index.ts"
      to: "src/database/tables/BehaviorRule.ts"
      via: "interface property definition"
      pattern: "behavior_rule: BehaviorRuleTable"
    - from: "src/tests/verify-phase-1.ts"
      to: "src/database/index.ts"
      via: "Database type usage in verification"
      pattern: "Database"
---

<objective>
Evolve the database schema to support Configurable Workspace Behaviors (CWB). 
This involves creating the `behavior_rule` table, wiping legacy `auto_action` data, 
and ensuring full type safety with Kysely.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/phase-1/RESEARCH.md
@src/database/index.ts
@src/database/runMigration.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Schema and Kysely Types</name>
  <files>database/migration_cwb_init.sql, src/database/tables/BehaviorRule.ts, src/database/index.ts</files>
  <action>
    1. Create `database/migration_cwb_init.sql`:
       - Define `behavior_rule` table with: `id` (UUID PK), `fk_project_id` (UUID NOT NULL FK), `name` (TEXT), `is_active` (BOOL), `behavior_type` (TEXT), `fk_task_id` (UUID FK), `criteria_field` (TEXT), `criteria_operator` (TEXT), `criteria_value` (TEXT), `action_message` (TEXT), `action_value` (TEXT), `version` (INT), `created_at`, `updated_at`.
       - Add `TRUNCATE auto_action CASCADE;` to wipe legacy data.
    2. Create `src/database/tables/BehaviorRule.ts`:
       - Define `BehaviorRuleTable` interface and `BehaviorType` / `CriteriaOperator` unions per RESEARCH.md.
    3. Update `src/database/index.ts`:
       - Add `behavior_rule: BehaviorRuleTable` to the `Database` interface.
  </action>
  <verify>
    <automated>grep -i "CREATE TABLE behavior_rule" database/migration_cwb_init.sql && bun x tsc --noEmit</automated>
  </verify>
  <done>Schema and types defined; project compiles successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Migration Execution and Verification Script</name>
  <files>src/tests/verify-phase-1.ts, src/database/runMigration.ts</files>
  <action>
    1. Create `src/tests/verify-phase-1.ts` to check if `behavior_rule` exists and `auto_action` is empty.
    2. Update `src/database/runMigration.ts` (if needed) to execute the new migration file.
    3. Execute the migration and the verification script.
  </action>
  <verify>
    <automated>bun run src/database/runMigration.ts && bun run src/tests/verify-phase-1.ts</automated>
  </verify>
  <done>Migration applied and verified against the live database.</done>
</task>

<task type="auto">
  <name>Task 3: Validation Documentation and Knowledge Graph Sync</name>
  <files>.planning/phases/01-database-evolution/VALIDATION.md</files>
  <action>
    1. Update `.planning/phases/01-database-evolution/VALIDATION.md` with the results from Task 2 (mark checkboxes [x]).
    2. Run `graphify update .` to ensure the knowledge graph reflects the new database files and relationships.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-database-evolution/VALIDATION.md && graphify update .</automated>
  </verify>
  <done>Phase 1 validated and knowledge graph updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB Migration | SQL execution crosses the application-database boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Migration Script | mitigate | SQL is strictly reviewed; uses `CASCADE` safely. |
| T-01-02 | Denial of Service | `TRUNCATE` | accept | Intentional data wipe per "Fresh Start" strategy. |
| T-01-SC | Tampering | npm installs | mitigate | No new packages; existing stack verified. |
</threat_model>

<verification>
- Table `behavior_rule` exists.
- `auto_action` is empty.
- `VALIDATION.md` is updated.
- `graphify update .` executed.
</verification>

<success_criteria>
Phase 1 is complete with a clean database schema and full type safety for the new CWB system.
</success_criteria>

<output>
Create `.planning/phases/01-database-evolution/01-01-SUMMARY.md` when done
</output>
