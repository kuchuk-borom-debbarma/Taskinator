So I want to create two kind of endpoint one for user or user facing and another for internal, which is not going to be accessible by the public, but only invocable by the services internally. So for this, I'm going to be researching about zero trust models and design designs.
Okay, so I figured out the way to do this. The way we are going to do. It is by signing each service. A password in a way the password is going to be stored as an environment variable of identity service. Now whenever a service wants to get a valid token. It has to hit this endpoint and pass the correct service name and the service password if this matches with the values that is stored in the environment of the identity service, then it is going to return a valid token which it can use to then make additional request as a service.
