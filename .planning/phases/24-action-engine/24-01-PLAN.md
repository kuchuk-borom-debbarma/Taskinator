---
phase: 24-action-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "modular-monolith/src/modules/autopilot/action-engine/types.ts",
  "modular-monolith/src/modules/autopilot/action-engine/ActionHasher.ts",
  "modular-monolith/src/modules/autopilot/action-engine/ActionHasher.test.ts",
  "modular-monolith/src/modules/autopilot/action-engine/ActionRepository.ts"
]
autonomous: true
requirements: [ACT-01]

must_haves:
  truths:
    - "Action AST can be deterministically hashed (structural deduplication)"
    - "Actions and their labels can be persisted and retrieved from the database"
  artifacts:
    - path: "modular-monolith/src/modules/autopilot/action-engine/types.ts"
      provides: "Action AST and Step definitions"
    - path: "modular-monolith/src/modules/autopilot/action-engine/ActionHasher.ts"
      provides: "SHA-256 structural hashing"
    - path: "modular-monolith/src/modules/autopilot/action-engine/ActionRepository.ts"
      provides: "Persistence mapping for actions and labels"
  key_links:
    - from: "ActionRepository.ts"
      to: "ActionHasher.ts"
      via: "getActionHash()"
---

<objective>
Define the core data structures and persistence layer for the Action Engine. This includes the Action AST types, deterministic structural hashing, and the repository for managing actions and project-level labels.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-CONTEXT.md
@.planning/phases/24-action-engine/RESEARCH.md
@modular-monolith/database/schema.sql
@modular-monolith/src/modules/autopilot/condition-engine/ConditionHasher.ts
@modular-monolith/src/modules/autopilot/condition-engine/ConditionRepository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Action AST types</name>
  <files>modular-monolith/src/modules/autopilot/action-engine/types.ts</files>
  <action>
    Define the types for the Action Engine AST per D-01:
    - `ActionStep`: { target: string, field: string, operation: 'set' | 'unset', value?: any }
    - `ActionAST`: ActionStep[] (a sequence of steps)
    - Export helper type guards if necessary.
  </action>
  <verify>
    <automated>
      tsc --noEmit modular-monolith/src/modules/autopilot/action-engine/types.ts && \
      graphify update . && \
      bun run format
    </automated>
  </verify>
  <done>Action AST types defined and exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ActionHasher and Tests</name>
  <files>modular-monolith/src/modules/autopilot/action-engine/ActionHasher.ts, modular-monolith/src/modules/autopilot/action-engine/ActionHasher.test.ts</files>
  <behavior>
    - Hash is SHA-256 hex string.
    - Deterministic: Same AST with different key order produces same hash.
    - Stable: Different ASTs produce different hashes.
  </behavior>
  <action>
    Implement `getActionHash(ast: ActionAST): string` mirroring `ConditionHasher.ts`.
    - Use `safe-stable-stringify` for deterministic JSON.
    - Use `node:crypto`'s `createHash('sha256')` to generate the hex digest.
    - Create `ActionHasher.test.ts` to verify deterministic output.
  </action>
  <verify>
    <automated>
      bun test modular-monolith/src/modules/autopilot/action-engine/ActionHasher.test.ts && \
      graphify update . && \
      bun run format
    </automated>
  </verify>
  <done>Structural hashing implemented and verified for Action ASTs.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ActionRepository</name>
  <files>modular-monolith/src/modules/autopilot/action-engine/ActionRepository.ts</files>
  <action>
    Implement `ActionRepository` mirroring `ConditionRepository.ts` per ACT-01:
    - `saveAction(labelName, ast, projectId, userId)`: inserts into `actions` (on conflict do nothing) and upserts into `action_labels`.
    - `getActionByLabel(labelName, projectId)`: retrieves AST by label.
    - `getActionByHash(hash)`: retrieves AST by hash.
    Use `db` from `../../../database/index.js`.
  </action>
  <verify>
    <automated>
      tsc --noEmit modular-monolith/src/modules/autopilot/action-engine/ActionRepository.ts && \
      graphify update . && \
      bun run format
    </automated>
  </verify>
  <done>Persistence layer for Actions implemented and verified by type-checking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → ActionRepository | Loaded AST must be validated as valid JSON sequences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | ActionHasher | mitigate | Use stable stringify to prevent hash bypass via key reordering. |
| T-24-SC | Tampering | npm installs | mitigate | slopcheck used during research for safe-stable-stringify. |
</threat_model>

<verification>
Verify types compile, hashing is deterministic via tests, and repository logic matches patterns.
</verification>

<success_criteria>
1. `ActionAST` supports sequence of steps (ACT-01).
2. `ActionHasher` produces identical hashes for identical logic regardless of key order.
3. `ActionRepository` correctly maps project-specific labels to structural actions.
</success_criteria>

<output>
Create `.planning/phases/24-action-engine/24-01-SUMMARY.md` when done
</output>
