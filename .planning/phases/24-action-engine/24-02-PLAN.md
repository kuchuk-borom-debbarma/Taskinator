---
phase: 24-action-engine
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified: [
  "modular-monolith/src/modules/autopilot/action-engine/ContextualEntity.ts",
  "modular-monolith/src/modules/autopilot/action-engine/AsyncResolverRegistry.ts",
  "modular-monolith/src/modules/autopilot/action-engine/defaultResolvers.ts"
]
autonomous: true
requirements: [ACT-02, ACT-03]

must_haves:
  truths:
    - "Entities are wrapped in a wrapper providing .get() and .set() API"
    - "Changes are tracked as 'dirty fields' for optimized persistence"
    - "Related entities (parent, project, etc.) are resolved lazily on demand"
  artifacts:
    - path: "modular-monolith/src/modules/autopilot/action-engine/ContextualEntity.ts"
      provides: "Getter/Setter API and dirty tracking"
    - path: "modular-monolith/src/modules/autopilot/action-engine/AsyncResolverRegistry.ts"
      provides: "Lazy resolution framework"
    - path: "modular-monolith/src/modules/autopilot/action-engine/defaultResolvers.ts"
      provides: "Resolvers for parent, project, team, teamMember"
  key_links:
    - from: "ContextualEntity.ts"
      to: "AsyncResolverRegistry.ts"
      via: "resolve(target) method"
---

<objective>
Implement the runtime entity wrapper and lazy resolution system. This provides the structured API for mutating state and the mechanism for fetching related context (e.g., parent task) only when an action step requires it.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-CONTEXT.md
@.planning/phases/24-action-engine/RESEARCH.md
@modular-monolith/src/modules/autopilot/action-engine/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ContextualEntity</name>
  <files>modular-monolith/src/modules/autopilot/action-engine/ContextualEntity.ts</files>
  <action>
    Implement the `ContextualEntity` class per ACT-03 and D-03:
    - Constructor takes `type`, `id`, `data`, and a reference to `AsyncResolverRegistry`.
    - `get(field: string): any`: returns value from data.
    - `set(field: string, value: any): void`: updates data and marks field as dirty.
    - `resolve(target: string): Promise<ContextualEntity>`: uses registry to fetch target, with local caching in `Map<string, ContextualEntity>`.
    - `getChanges(): Record<string, any>`: returns only dirty fields and their new values.
  </action>
  <verify>
    <automated>tsc --noEmit modular-monolith/src/modules/autopilot/action-engine/ContextualEntity.ts</automated>
  </verify>
  <done>ContextualEntity wrapper implemented with dirty tracking and resolve support.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AsyncResolverRegistry and Default Resolvers</name>
  <files>modular-monolith/src/modules/autopilot/action-engine/AsyncResolverRegistry.ts, modular-monolith/src/modules/autopilot/action-engine/defaultResolvers.ts</files>
  <action>
    Implement lazy resolution system per ACT-02 and D-02:
    1. `AsyncResolverRegistry`: Registry mapping `sourceType:target` to `ResolverFn`.
    2. `defaultResolvers.ts`: Implement and register standard resolvers using Kysely `db`:
       - `project_task -> parent` (fetches parent task)
       - `project_task -> project`
       - `project -> team`
       - `project_team -> teamMember` (or appropriate hierarchy based on schema)
    Resolvers should wrap fetched data in a new `ContextualEntity`.
  </action>
  <verify>
    <automated>tsc --noEmit modular-monolith/src/modules/autopilot/action-engine/AsyncResolverRegistry.ts modular-monolith/src/modules/autopilot/action-engine/defaultResolvers.ts</automated>
  </verify>
  <done>Lazy resolution registry and default resolvers implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ContextualEntity → Registry | Resolver results must be trusted and wrapped in ContextualEntity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02 | Availability | ContextualEntity | mitigate | Local cache in `resolvedTargets` prevents infinite resolver loops and N+1. |
| T-24-03 | Elevation | Resolvers | mitigate | Ensure resolvers honor project boundaries during fetch (ACT-02). |
</threat_model>

<verification>
Verify that `ContextualEntity` tracks dirty fields correctly and `resolve` calls the registry.
</verification>

<success_criteria>
1. `ContextualEntity` provides `.get()` and `.set()` API (ACT-03).
2. Dirty field tracking captures all mutations.
3. Lazy resolution successfully fetches related entities on demand (ACT-02).
</success_criteria>

<output>
Create `.planning/phases/24-action-engine/24-02-SUMMARY.md` when done
</output>
