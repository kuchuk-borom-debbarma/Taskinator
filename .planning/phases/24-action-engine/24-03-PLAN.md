---
phase: 24-action-engine
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified: [
  "modular-monolith/src/modules/autopilot/action-engine/ActionExecutor.ts",
  "modular-monolith/src/modules/autopilot/action-engine/ActionEngine.test.ts"
]
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03]

must_haves:
  truths:
    - "Action sequence executes steps in order"
    - "Execution returns a list of mutated entities for persistence"
    - "Lazy resolution is triggered only when a step targets a related entity"
  artifacts:
    - path: "modular-monolith/src/modules/autopilot/action-engine/ActionExecutor.ts"
      provides: "Main logic for running Action ASTs"
    - path: "modular-monolith/src/modules/autopilot/action-engine/ActionEngine.test.ts"
      provides: "Full engine verification"
  key_links:
    - from: "ActionExecutor.ts"
      to: "ContextualEntity.ts"
      via: ".resolve() and .set() calls"
---

<objective>
Build the core executor that interprets and runs Action AST sequences. This task integrates the lazy resolution and contextual wrapper systems to provide a complete pipeline for automated entity mutation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-CONTEXT.md
@.planning/phases/24-action-engine/RESEARCH.md
@modular-monolith/src/modules/autopilot/action-engine/types.ts
@modular-monolith/src/modules/autopilot/action-engine/ContextualEntity.ts
@modular-monolith/src/modules/autopilot/action-engine/AsyncResolverRegistry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ActionExecutor</name>
  <files>modular-monolith/src/modules/autopilot/action-engine/ActionExecutor.ts</files>
  <action>
    Implement `ActionExecutor` per D-01 and D-02:
    - `execute(ast: ActionAST, self: ContextualEntity): Promise<ContextualEntity[]>`:
      - Iterates through `ActionStep` sequence.
      - Calls `target = await self.resolve(step.target)`.
      - Performs `target.set(step.field, step.value)` (or unset).
      - Tracks all modified `ContextualEntity` instances in a Set.
      - Returns the list of modified entities.
    Ensure it does NOT perform database updates itself (per Pitfall 2).
  </action>
  <verify>
    <automated>
      tsc --noEmit modular-monolith/src/modules/autopilot/action-engine/ActionExecutor.ts && \
      graphify update . && \
      bun run format
    </automated>
  </verify>
  <done>ActionExecutor implemented and handles Target-Field-Value sequence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Action Engine Tests</name>
  <files>modular-monolith/src/modules/autopilot/action-engine/ActionEngine.test.ts</files>
  <behavior>
    - Test 1: Sequence of 'set' operations on 'self' correctly tracks dirty fields.
    - Test 2: 'set' operation on 'parent' triggers lazy resolution via registry.
    - Test 3: Multiple steps targeting same entity only resolve it once (caching).
    - Test 4: Executor returns only the entities that were actually modified.
    - Test 5: Verify structural hashing (from Task 1) produces same ID for reordered JSON keys.
  </behavior>
  <action>
    Write comprehensive tests for the Action Engine. Use mocks for the `AsyncResolverRegistry` to verify lazy resolution triggers.
  </action>
  <verify>
    <automated>
      bun test modular-monolith/src/modules/autopilot/action-engine/ActionEngine.test.ts && \
      graphify update . && \
      bun run format
    </automated>
  </verify>
  <done>Action Engine verified with unit and integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ActionExecutor → ContextualEntity | Executor must only use the public .set() and .resolve() methods |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering | ActionExecutor | mitigate | Executor only mutates wrapped entities; final persistence is handled by caller within transaction. |
</threat_model>

<verification>
Run the automated test suite to verify all engine behaviors.
</verification>

<success_criteria>
1. Action steps execute in strict sequence.
2. Lazy resolution only fetches what is needed.
3. Mutations are correctly tracked and returned for atomic persistence.
</success_criteria>

<output>
Create `.planning/phases/24-action-engine/24-03-SUMMARY.md` when done
</output>
