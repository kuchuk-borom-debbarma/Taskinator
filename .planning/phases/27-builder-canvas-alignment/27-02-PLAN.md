---
phase: 27-builder-canvas-alignment
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified: [
  ui-v1/src/components/Autopilot/Pipeline/PipelineEditor.tsx,
  ui-v1/src/components/Autopilot/Builder/ConditionStepCard.tsx,
  ui-v1/src/components/Autopilot/Pipeline/ActionPipelineEditor.tsx
]
autonomous: true
requirements: [UI-PIPE-01, UI-PIPE-02]
must_haves:
  truths:
    - "PipelineEditor supports mixed Condition and Action steps"
    - "Reordering Condition blocks between Action blocks is possible via DND"
  artifacts:
    - path: "ui-v1/src/components/Autopilot/Builder/ConditionStepCard.tsx"
      provides: "UI wrapper for Logic Blocks in pipeline"
    - path: "ui-v1/src/components/Autopilot/Pipeline/PipelineEditor.tsx"
      provides: "Unified pipeline orchestration"
---

<objective>
Implement interleaved pipeline UI with Condition and Action blocks.

Purpose: Allow complex logic to be sequenced within the Autopilot pipeline.
Output: PipelineEditor component and ConditionStepCard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-CONTEXT.md
@.planning/phases/27-builder-canvas-alignment/RESEARCH.md
@ui-v1/src/components/Autopilot/Pipeline/ActionPipelineEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConditionStepCard</name>
  <files>ui-v1/src/components/Autopilot/Builder/ConditionStepCard.tsx</files>
  <action>
    Implement `ConditionStepCard` to represent a `ConditionNode` block in the pipeline list.
    Features:
    - Summary view: Show a brief description or "Logic Block" label.
    - Mini Canvas: Optional read-only XYFlow view (per research discretion).
    - Edit button: Open `ConditionBuilderCanvas` in a modal.
    - Remove button: Delete block from pipeline.
  </action>
  <verify>
    <automated>npm test ConditionStepCard</automated>
  </verify>
  <done>Condition blocks can be rendered and edited in isolation</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ActionPipelineEditor to PipelineEditor</name>
  <files>
    ui-v1/src/components/Autopilot/Pipeline/PipelineEditor.tsx,
    ui-v1/src/components/Autopilot/Pipeline/ActionPipelineEditor.tsx
  </files>
  <action>
    Rename `ActionPipelineEditor.tsx` to `PipelineEditor.tsx`.
    Update props to accept `steps: PipelineStep[]`.
    Refactor rendering loop to handle both `AutopilotAction` and `AutopilotCondition`.
    - If action → render `ActionStepCard`.
    - If condition → render `ConditionStepCard`.
    Maintain `StepConnector` between all items.
  </action>
  <verify>
    <automated>npm test PipelineEditor</automated>
  </verify>
  <done>Unified PipelineEditor handles mixed content</done>
</task>

<task type="auto">
  <name>Task 3: Implement DND Reordering</name>
  <files>ui-v1/src/components/Autopilot/Pipeline/PipelineEditor.tsx</files>
  <action>
    Wrap pipeline items in `Reorder.Group` and `Reorder.Item` from `framer-motion`.
    Ensure reordering updates the `pipeline` array indices.
    Per UI-PIPE-02: Support moving "Condition Block" between "Action Blocks".
  </action>
  <verify>
    <automated>npm test PipelineEditor -- reorder</automated>
  </verify>
  <done>User can reorder any step in the pipeline via drag-and-drop</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → State | Reordering must preserve AST integrity |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02 | Integrity | PipelineEditor | mitigate | Validate pipeline sequence after every reorder operation |
</threat_model>

<verification>
Verify mixed list rendering and reordering functionality.
</verification>

<success_criteria>
Interleaved pipeline rendered with DND support for both logic and action blocks.
</success_criteria>

<output>
Create `.planning/phases/27-builder-canvas-alignment/27-02-SUMMARY.md` when done
</output>
