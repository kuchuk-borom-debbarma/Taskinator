---
phase: 27-builder-canvas-alignment
plan: 02
type: execute
wave: 3
depends_on: [27-01]
files_modified: [
  ui-v1/src/components/Autopilot/Pipeline/PipelineEditor.tsx,
  ui-v1/src/components/Autopilot/Pipeline/PipelineEditor.test.tsx,
  ui-v1/src/components/Autopilot/Builder/ConditionStepCard.tsx,
  ui-v1/src/components/Autopilot/Builder/ConditionStepCard.test.tsx,
  ui-v1/src/components/Autopilot/Pipeline/PipelineStepConnector.tsx
]
autonomous: true
requirements: [UI-CTX-03, UI-PIPE-01, UI-PIPE-02, UI-PIPE-03]
must_haves:
  truths:
    - "PipelineEditor supports mixed Condition and Action steps"
    - "UI provides buttons to add both Conditions and Actions"
    - "DND reordering for all pipeline steps implemented"
    - "Visual flow indicators for sequential semantics present"
  artifacts:
    - path: "ui-v1/src/components/Autopilot/Builder/ConditionStepCard.tsx"
      provides: "UI wrapper for Logic Blocks"
  key_links:
    - from: "PipelineEditor"
      to: "ConditionStepCard"
      via: "step rendering"
---

<objective>
Implement interleaved pipeline UI with Condition and Action blocks.

Purpose: Allow complex logic to be sequenced within the Autopilot pipeline.
Output: PipelineEditor component and ConditionStepCard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-CONTEXT.md
@.planning/phases/27-builder-canvas-alignment/RESEARCH.md
@ui-v1/src/components/Autopilot/Pipeline/ActionPipelineEditor.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ConditionStepCard</name>
  <files>
    ui-v1/src/components/Autopilot/Builder/ConditionStepCard.tsx,
    ui-v1/src/components/Autopilot/Builder/ConditionStepCard.test.tsx
  </files>
  <action>
    Implement `ConditionStepCard` to represent a `ConditionNode` block in the pipeline list.
  </action>
  <verify>
    <automated>npm test ConditionStepCard</automated>
  </verify>
  <done>Condition blocks can be rendered and edited</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Unified PipelineEditor</name>
  <files>
    ui-v1/src/components/Autopilot/Pipeline/PipelineEditor.tsx,
    ui-v1/src/components/Autopilot/Pipeline/PipelineEditor.test.tsx
  </files>
  <action>
    Refactor `ActionPipelineEditor.tsx` into `PipelineEditor.tsx` to support mixed steps.
    Include "Add Condition" and "Add Action" buttons.
  </action>
  <verify>
    <automated>npm test PipelineEditor</automated>
  </verify>
  <done>Unified PipelineEditor handles mixed content</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Pipeline Reordering</name>
  <files>ui-v1/src/components/Autopilot/Pipeline/PipelineEditor.tsx</files>
  <action>
    Use `framer-motion` `Reorder` component to allow reordering of both actions and conditions in the pipeline.
  </action>
  <verify>
    <automated>npm test PipelineEditor -- reorder</automated>
  </verify>
  <done>Full pipeline reordering enabled</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement Visual Flow Indicators</name>
  <files>
    ui-v1/src/components/Autopilot/Pipeline/PipelineStepConnector.tsx,
    ui-v1/src/components/Autopilot/Pipeline/PipelineEditor.tsx
  </files>
  <action>
    Implement visual indicators (e.g., specific arrows or status labels) that explain "halt-on-false" sequential semantics between logic blocks and actions.
  </action>
  <verify>
    <automated>npm test PipelineEditor -- flow</automated>
  </verify>
  <done>Sequential semantics visually communicated</done>
</task>

</tasks>

<success_criteria>
Interleaved pipeline rendered with both logic and action blocks, reordering, and flow visibility.
</success_criteria>

<output>
Create `.planning/phases/27-builder-canvas-alignment/27-02-SUMMARY.md` when done
</output>
