---
phase: 27-builder-canvas-alignment
plan: 00
type: execute
wave: 1
depends_on: []
files_modified: [
  modular-monolith/database/schema.sql,
  modular-monolith/src/graphql/resolvers/autopilot.ts
]
autonomous: true
requirements: [GQL-01]
must_haves:
  truths:
    - "Autopilot table has triggers JSONB column"
    - "Autopilot.triggers resolver returns persisted triggers"
  artifacts:
    - path: "modular-monolith/database/schema.sql"
      provides: "Updated schema"
---

<objective>
Implement backend persistence for Autopilot triggers.

Purpose: Ensure the 'triggers' array is saved and returned by the API.
Output: Updated database schema and GQL resolvers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@modular-monolith/database/schema.sql
@modular-monolith/src/graphql/resolvers/autopilot.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add triggers column to database</name>
  <files>modular-monolith/database/schema.sql</files>
  <action>
    Add `triggers JSONB NOT NULL DEFAULT '[]'` to the `autopilot` table in `schema.sql`.
  </action>
  <verify>
    <automated>grep -q "triggers JSONB" modular-monolith/database/schema.sql</automated>
  </verify>
  <done>Database schema updated</done>
</task>

<task type="auto">
  <name>Task 2: Update Autopilot Resolvers</name>
  <files>modular-monolith/src/graphql/resolvers/autopilot.ts</files>
  <action>
    - Update `Autopilot.triggers` resolver to return `parent.triggers`.
    - Ensure `createAutopilot` and `updateAutopilot` mutations save the `triggers` input to the DB.
  </action>
  <verify>
    <automated>grep -q "triggers: (parent" modular-monolith/src/graphql/resolvers/autopilot.ts</automated>
  </verify>
  <done>Resolvers and mutations handle triggers correctly</done>
</task>

</tasks>

<success_criteria>
Triggers are persisted in the database and returned via GraphQL.
</success_criteria>

<output>
Create `.planning/phases/27-builder-canvas-alignment/27-00-SUMMARY.md` when done
</output>
