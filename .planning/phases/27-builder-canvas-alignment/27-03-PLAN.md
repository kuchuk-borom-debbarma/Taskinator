---
phase: 27-builder-canvas-alignment
plan: 03
type: execute
wave: 3
depends_on: [27-02]
files_modified: [
  ui-v1/src/components/Autopilot/CreateAutopilotModal.tsx,
  ui-v1/src/components/Autopilot/Builder/PredicateEditorPanel.tsx,
  ui-v1/src/components/Autopilot/Pipeline/ActionConfigForm.tsx
]
autonomous: true
requirements: [UI-CTX-02, UI-PIPE-01]
must_haves:
  truths:
    - "CreateAutopilotModal uses interleaved PipelineEditor"
    - "Predicate dropdowns filtered by selected triggerEntityType"
    - "Action parameters filtered by selected triggerEntityType"
  artifacts:
    - path: "ui-v1/src/components/Autopilot/CreateAutopilotModal.tsx"
      provides: "Sequential wizard flow"
  key_links:
    - from: "CreateAutopilotModal"
      to: "AutopilotTriggerProvider"
      via: "trigger locking"
---

<objective>
Refactor Autopilot creation wizard for sequential flow and entity-awareness.

Purpose: Align the wizard UX with the v6.0 data model and ensure context-locked editing.
Output: Updated CreateAutopilotModal and filtered editor panels.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-CONTEXT.md
@.planning/phases/27-builder-canvas-alignment/RESEARCH.md
@ui-v1/src/components/Autopilot/CreateAutopilotModal.tsx
@ui-v1/src/components/Autopilot/Builder/PredicateEditorPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Trigger-Locked Context in Wizard</name>
  <files>ui-v1/src/components/Autopilot/CreateAutopilotModal.tsx</files>
  <action>
    Refactor `CreateAutopilotModal` to use a 2-step flow:
    1. Select Trigger (Event + Entity). Sets `selectedEntityType` in `AutopilotTriggerContext`.
    2. Build Pipeline (Interleaved Logic + Actions).
    Lock the trigger selection once pipeline has steps to prevent context mismatches.
  </action>
  <verify>
    <automated>npm test CreateAutopilotModal</automated>
  </verify>
  <done>Trigger selection locks the downstream context</done>
</task>

<task type="auto">
  <name>Task 2: Filter Predicate & Action Fields by Entity</name>
  <files>
    ui-v1/src/components/Autopilot/Builder/PredicateEditorPanel.tsx,
    ui-v1/src/components/Autopilot/Pipeline/ActionConfigForm.tsx
  </files>
  <action>
    Update panels to consume `AutopilotMetadataContext`.
    Use `selectedEntityType` from `AutopilotTriggerContext` to filter available fields and operators.
    Per UI-CTX-02: Dropdowns populated dynamically from MetadataContext.
  </action>
  <verify>
    <automated>npm test PredicateEditorPanel</automated>
  </verify>
  <done>UI only shows fields valid for the chosen trigger entity</done>
</task>

<task type="auto">
  <name>Task 3: Wiring & Final Validation</name>
  <files>ui-v1/src/components/Autopilot/CreateAutopilotModal.tsx</files>
  <action>
    Update `onSave` to use the unified `pipeline` array (Action | Condition).
    Ensure `createAutopilot` mutation input matches GQL structure: `pipeline: [{ action: ... }, { condition: ... }]`.
    Verify all references to legacy `conditions` and `actions` are removed.
  </action>
  <verify>
    <automated>npm test CreateAutopilotModal -- integration</automated>
  </verify>
  <done>Full creation flow aligned with sequential model</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Wizard → Metadata | Filtering must be enforced at the UI layer to prevent invalid rule creation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Information Disclosure | MetadataContext | accept | Metadata only contains field labels and operators, no sensitive data |
</threat_model>

<verification>
Perform end-to-end creation of an Autopilot with mixed steps and verify field filtering.
</verification>

<success_criteria>
Wizard refactored. Fields filtered by entity. Pipeline correctly serialized for save.
</success_criteria>

<output>
Create `.planning/phases/27-builder-canvas-alignment/27-03-SUMMARY.md` when done
</output>
