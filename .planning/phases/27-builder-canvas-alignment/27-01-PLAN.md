---
phase: 27-builder-canvas-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  ui-v1/src/components/Autopilot/AutopilotMetadataContext.tsx,
  ui-v1/src/components/Autopilot/AutopilotTriggerContext.tsx,
  ui-v1/src/components/Autopilot/Builder/treeSerializer.ts,
  ui-v1/src/components/Autopilot/Builder/treeDeserializer.ts,
  ui-v1/src/components/Autopilot/AutopilotDashboardView.tsx
]
autonomous: true
requirements: [UI-CTX-02]
must_haves:
  truths:
    - "AutopilotMetadataContext fetches metadata from backend"
    - "Serializers produce/consume GQL Union typed objects (__typename)"
    - "Metadata provided at Dashboard level"
  artifacts:
    - path: "ui-v1/src/components/Autopilot/AutopilotMetadataContext.tsx"
      provides: "Metadata Provider"
    - path: "ui-v1/src/components/Autopilot/Builder/treeSerializer.ts"
      provides: "__typename aware serialization"
  key_links:
    - from: "AutopilotDashboardView"
      to: "AutopilotMetadataProvider"
      via: "context wrapping"
---

<objective>
Setup core context and serialization for interleaved Autopilot model.

Purpose: Ensure UI uses GQL-standard structures and is aware of entity-specific metadata.
Output: Context providers and updated serializers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-CONTEXT.md
@.planning/phases/27-builder-canvas-alignment/RESEARCH.md
@ui-v1/src/components/Autopilot/Builder/treeSerializer.ts
@ui-v1/src/components/Autopilot/Builder/treeDeserializer.ts
@ui-v1/src/components/Autopilot/AutopilotDashboardView.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Metadata & Trigger Contexts</name>
  <files>
    ui-v1/src/components/Autopilot/AutopilotMetadataContext.tsx,
    ui-v1/src/components/Autopilot/AutopilotTriggerContext.tsx
  </files>
  <behavior>
    - MetadataContext: Fetch `autopilotMetadata(entityType: "task")` (default) via GQL.
    - MetadataContext: Expose `getFieldsForEntity(entityType)` helper.
    - TriggerContext: Track `selectedEntityType` ('task'|'project'|'team').
  </behavior>
  <action>
    Implement React contexts. `AutopilotMetadataProvider` should use `useQuery` to fetch `GET_AUTOPILOT_METADATA`.
    `AutopilotTriggerProvider` manage state for current trigger type.
  </action>
  <verify>
    <automated>npm test AutopilotMetadataContext</automated>
  </verify>
  <done>Contexts exist and provide expected values</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Serializers for GQL Unions</name>
  <files>
    ui-v1/src/components/Autopilot/Builder/treeSerializer.ts,
    ui-v1/src/components/Autopilot/Builder/treeDeserializer.ts
  </files>
  <behavior>
    - `graphToConditionTree`: Return objects with `__typename: 'AndNode' | 'OrNode' | 'NotNode' | 'PredicateNode'`.
    - `conditionTreeToGraph`: Consume objects with these typenames.
  </behavior>
  <action>
    Refactor serializers to align with `ConditionNode` GQL union. Replace internal 'predicate'/'and' types with `__typename`.
    Ensure property names match GQL exactly (e.g. `children` for And/Or, `child` for Not).
  </action>
  <verify>
    <automated>npm test treeSerializer</automated>
  </verify>
  <done>Serializers handle GQL union structure correctly</done>
</task>

<task type="auto">
  <name>Task 3: Wrap Dashboard in Metadata Provider</name>
  <files>ui-v1/src/components/Autopilot/AutopilotDashboardView.tsx</files>
  <action>
    Import `AutopilotMetadataProvider` and `AutopilotTriggerProvider`.
    Wrap main view content in `AutopilotMetadataProvider`.
    Per D-01: Metadata fetched once at dashboard root.
  </action>
  <verify>
    <automated>npm test AutopilotDashboardView</automated>
  </verify>
  <done>Dashboard provides metadata context to children</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Serializer → API | AST sent to backend must follow strict GQL types |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | treeSerializer | mitigate | Enforce `__typename` using generated GQL types |
</threat_model>

<verification>
Verify contexts correctly provide metadata and serializers produce GQL-compatible objects.
</verification>

<success_criteria>
Metadata fetched and available via context. Serializers updated to GQL union format.
</success_criteria>

<output>
Create `.planning/phases/27-builder-canvas-alignment/27-01-SUMMARY.md` when done
</output>
