---
phase: 23-condition-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "modular-monolith/package.json",
  "modular-monolith/src/modules/autopilot/internal/OperatorRegistry.ts",
  "modular-monolith/src/modules/autopilot/internal/ConditionHasher.ts",
  "modular-monolith/src/modules/autopilot/internal/ConditionEvaluator.ts",
  "modular-monolith/src/modules/autopilot/internal/ConditionRepository.ts",
  "modular-monolith/src/modules/autopilot/internal/state/StateFetcher.ts",
  "modular-monolith/src/modules/autopilot/internal/__tests__/ConditionEngine.test.ts"
]
autonomous: false
requirements: [COND-01, COND-02, COND-03, COND-05, COND-06]
user_setup: []

must_haves:
  truths:
    - "Conditions are deduplicated by structural hash upon storage"
    - "Evaluator correctly handles AND/OR/NOT logic"
    - "Change-based operators compare 'is' and 'was' states"
    - "StateFetcher provides fresh 'is' state from database"
  artifacts:
    - path: "modular-monolith/src/modules/autopilot/internal/ConditionEvaluator.ts"
      provides: "Boolean logic and operator execution"
    - path: "modular-monolith/src/modules/autopilot/internal/ConditionHasher.ts"
      provides: "Deterministic hashing via safe-stable-stringify"
    - path: "modular-monolith/src/modules/autopilot/internal/OperatorRegistry.ts"
      provides: "Extensible registry for condition operators"
  key_links:
    - from: "ConditionEvaluator.ts"
      to: "OperatorRegistry.ts"
      via: "Operator execution"
    - from: "ConditionRepository.ts"
      to: "ConditionHasher.ts"
      via: "Deduplication check"
---

<objective>
Implement the core Condition Engine for Autopilot, enabling entity-agnostic evaluation, structural hashing for deduplication, and boolean composability.

Purpose: Decouple business logic from automation rules and ensure efficient storage of identical conditions.
Output: A set of internal services and a test suite proving correctness of the evaluation logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package Legitimacy Check</name>
  <files>modular-monolith/package.json</files>
  <action>
    Verify the legitimacy of `safe-stable-stringify` (v2.5.0) on npm.
    This package is required for deterministic hashing of condition definitions (COND-06).
  </action>
  <verify>
    Human confirms package is safe.
  </verify>
  <done>Package verified</done>
</task>

<task type="auto">
  <name>Task 1: Setup Dependencies and Operator Registry</name>
  <files>
    modular-monolith/package.json,
    modular-monolith/src/modules/autopilot/internal/OperatorRegistry.ts
  </files>
  <action>
    1. Install `safe-stable-stringify` (per COND-06).
    2. Create `OperatorRegistry.ts` implementing a registry pattern (per D-05).
    3. Implement operators: `eq`, `neq`, `gt`, `lt`, `changed`, `changedFrom`, `changedTo`, `changedFromTo`.
    - Interface: `(is: any, was: any, value: any) => boolean`.
    - `changed` returns `is !== was`.
    - `changedFrom` returns `was === expected && is !== was`.
    - `changedTo` returns `is === expected && is !== was`.
  </action>
  <verify>
    <automated>bun run check-types</automated>
  </verify>
  <done>Registry implemented with all required operators.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Condition Hasher and Evaluator</name>
  <files>
    modular-monolith/src/modules/autopilot/internal/ConditionHasher.ts,
    modular-monolith/src/modules/autopilot/internal/ConditionEvaluator.ts
  </files>
  <action>
    1. Implement `ConditionHasher.ts` using `safe-stable-stringify` and `crypto.createHash('sha256')` to generate structural IDs.
    2. Implement `ConditionEvaluator.ts` (per D-01, D-02).
    - Support recursive evaluation for `AND`, `OR`, `NOT` (per COND-05).
    - For leaf conditions, use `lodash.get` to extract field values from `is` and `was` context.
    - Invoke operators from `OperatorRegistry`.
    - Add "tons of comments" as requested.
  </action>
  <verify>
    <automated>bun run check-types</automated>
  </verify>
  <done>Hashing and evaluation logic complete.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Persistence and State Fetcher</name>
  <files>
    modular-monolith/src/modules/autopilot/internal/ConditionRepository.ts,
    modular-monolith/src/modules/autopilot/internal/state/StateFetcher.ts
  </files>
  <action>
    1. Implement `ConditionRepository.ts` for saving conditions.
    - Use `ConditionHasher` to generate ID.
    - Use `ON CONFLICT (id) DO NOTHING` for deduplication.
    2. Implement `StateFetcher.ts` (per D-02).
    - Provide `fetchCurrentState(entityType, entityId)` to get fresh `is` state from DB.
    - This ensures evaluation happens against latest data.
  </action>
  <verify>
    <automated>bun run check-types</automated>
  </verify>
  <done>Data access and fresh state fetching implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Comprehensive Test Suite</name>
  <files>modular-monolith/src/modules/autopilot/internal/__tests__/ConditionEngine.test.ts</files>
  <behavior>
    - Test hashing: Same definition (different key order) results in same hash.
    - Test evaluation: `eq`, `neq`, `gt`, `lt` work on simple values.
    - Test change operators: `changed` logic correctly uses `was` and `is`.
    - Test boolean logic: Deeply nested AND/OR/NOT trees evaluate correctly.
  </behavior>
  <action>
    Create a Jest test suite for the condition engine components.
  </action>
  <verify>
    <automated>bun run test modular-monolith/src/modules/autopilot/internal/__tests__/ConditionEngine.test.ts</automated>
  </verify>
  <done>All tests pass with high coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted JSON condition definitions. |
| Evaluator -> Entity Data | Evaluator reads internal entity state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | ConditionEvaluator | mitigate | Strictly typed AST and operator lookup ensures only known operations run. |
| T-23-02 | Information Disclosure | ConditionEvaluator | mitigate | Field access via `lodash.get` should be restricted to an allow-list if exposed to end-users (future work). |
| T-23-SC | Tampering | npm installs | mitigate | Blocking human checkpoint for `safe-stable-stringify` verification. |
</threat_model>

<verification>
- Logic verified via unit tests in Task 4.
- Type safety verified via `bun run check-types`.
- Hashing consistency verified by feeding same-structure-different-order JSON to `ConditionHasher`.
</verification>

<success_criteria>
1. `ConditionHasher` produces identical IDs for semantically identical JSON.
2. `ConditionEvaluator` correctly handles AND/OR/NOT and all comparison operators.
3. `ConditionRepository` successfully deduplicates conditions in the database.
4. `StateFetcher` correctly retrieves fresh entity data.
</success_criteria>

<output>
Create `.planning/phases/23-condition-engine/23-01-SUMMARY.md` when done
</output>
