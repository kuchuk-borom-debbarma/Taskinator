---
phase: 23-condition-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modular-monolith/package.json
  - modular-monolith/src/modules/autopilot/condition-engine/types.ts
  - modular-monolith/src/modules/autopilot/condition-engine/OperatorRegistry.ts
  - modular-monolith/src/modules/autopilot/condition-engine/ConditionHasher.ts
  - modular-monolith/src/modules/autopilot/condition-engine/ConditionEvaluator.ts
autonomous: true
requirements: [COND-02, COND-03, COND-04, COND-05, COND-06]

must_haves:
  truths:
    - "Condition evaluation is entity-agnostic using a context object"
    - "Structural hashing produces deterministic IDs for identical logic"
    - "AND/OR/NOT logic composition is supported"
  artifacts:
    - path: "modular-monolith/src/modules/autopilot/condition-engine/ConditionEvaluator.ts"
      provides: "Core evaluation logic"
    - path: "modular-monolith/src/modules/autopilot/condition-engine/ConditionHasher.ts"
      provides: "Deterministic structural hashing"
  key_links:
    - from: "ConditionEvaluator.ts"
      to: "OperatorRegistry.ts"
      via: "operator lookup"
---

<objective>
Implement the core logic for the Condition Engine, including types, operator registry, structural hashing, and the recursive evaluator.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup types and install safe-stable-stringify</name>
  <files>modular-monolith/package.json, modular-monolith/src/modules/autopilot/condition-engine/types.ts</files>
  <action>
    1. Install `safe-stable-stringify` in `modular-monolith`.
    2. Create `types.ts` defining:
       - `ConditionAST`: Union of Leaf (field, operator, value) and Branch (logic, terms).
       - `EvaluationContext`: Interface with `is` and `was` record maps.
       - `OperatorFn`: Type for operator evaluation functions `(isVal: any, wasVal: any, expected: any) => boolean`.
  </action>
  <verify>
    <automated>cd modular-monolith && npm list safe-stable-stringify && bun run check-types</automated>
  </verify>
  <done>Dependencies installed and core types defined.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Operator Registry</name>
  <files>modular-monolith/src/modules/autopilot/condition-engine/OperatorRegistry.ts</files>
  <action>
    Implement a `ModularOperatorRegistry` that maps operator keys to `OperatorFn`.
    Include standard operators: `eq`, `neq`, `gt`, `lt`, `gte`, `lte`, `in`, `contains`, `empty`, `exists`.
    Include change-based operators (COND-03):
    - `changed`: `isVal !== wasVal`
    - `changedTo`: `isVal === expected && wasVal !== expected`
    - `changedFrom`: `wasVal === expected && isVal !== expected`
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/condition-engine/OperatorRegistry.test.ts</automated>
  </verify>
  <done>Operators implemented and passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Structural Hasher</name>
  <files>modular-monolith/src/modules/autopilot/condition-engine/ConditionHasher.ts</files>
  <action>
    Implement `getConditionHash(ast: ConditionAST): string`.
    1. Use `safe-stable-stringify` to get a deterministic JSON string of the AST.
    2. Return SHA-256 hash (hex) of the string using Node's `crypto` module.
    Ensure consistent hashing regardless of key order in the input object (per COND-06).
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/condition-engine/ConditionHasher.test.ts</automated>
  </verify>
  <done>Hasher produces identical IDs for semantically identical conditions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Condition Evaluator & Natural Language Generator</name>
  <files>modular-monolith/src/modules/autopilot/condition-engine/ConditionEvaluator.ts</files>
  <behavior>
    - Evaluate Leaf: {field: 'status', operator: 'eq', value: 'DONE'} against {is: {status: 'DONE'}} -> true.
    - Evaluate Logic: {logic: 'AND', terms: [...]} evaluates all terms.
    - Change Trigger: {field: 'priority', operator: 'changed'} against {is: {priority: 1}, was: {priority: 2}} -> true.
    - NL Generation: {field: 'status', operator: 'eq', value: 'DONE'} -> "status is DONE".
  </behavior>
  <action>
    1. Implement `evaluateCondition(ast, context)` recursively.
    2. Implement `toNaturalLanguage(ast)` (COND-04) that returns a human-readable prefix/postfix representation.
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/condition-engine/ConditionEvaluator.test.ts</automated>
  </verify>
  <done>Evaluator handles complex logic and change triggers; NL generator produces valid strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AST → Evaluator | Condition definitions must be validated against schema before evaluation to prevent prototype pollution or invalid property access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | Evaluator | mitigate | Ensure evaluator only accesses allowed fields in context. |
| T-23-02 | Denial of Service | Evaluator | mitigate | Limit recursion depth for boolean logic terms. |
</threat_model>

<verification>
1. `bun run check-types` passes.
2. `bun test` passes for all condition engine components.
3. `graphify update .` to ensure dependency graph reflects new engine.
4. `bun run format` to ensure style consistency.
</verification>

<success_criteria>
- Core logic for evaluation, hashing, and boolean composition is fully functional and tested.
- `toNaturalLanguage` produces expected human-readable output.
</success_criteria>

<output>
Create `.planning/phases/23-condition-engine/23-01-SUMMARY.md` when done
</output>
