---
phase: 23-condition-engine
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - modular-monolith/src/modules/autopilot/condition-engine/ConditionRepository.ts
  - modular-monolith/src/modules/autopilot/condition-engine/ContextBuilder.ts
autonomous: true
requirements: [COND-01, COND-06]

must_haves:
  truths:
    - "Structural deduplication ensures identical logic shares one DB record"
    - "Conditions are named and managed via condition_labels"
    - "Evaluation context uses fresh DB state for 'is' values"
  artifacts:
    - path: "modular-monolith/src/modules/autopilot/condition-engine/ConditionRepository.ts"
      provides: "Hash-aware persistence logic"
    - path: "modular-monolith/src/modules/autopilot/condition-engine/ContextBuilder.ts"
      provides: "Evaluation context construction"
  key_links:
    - from: "ConditionRepository.ts"
      to: "ConditionHasher.ts"
      via: "hash generation on save"
---

<objective>
Implement the persistence layer and state retrieval logic for the Condition Engine, enabling structural deduplication and named condition aliases.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-CONTEXT.md
@.planning/phases/23-condition-engine/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Condition Repository with Structural Deduplication</name>
  <files>modular-monolith/src/modules/autopilot/condition-engine/ConditionRepository.ts</files>
  <action>
    Implement `ConditionRepository` using Kysely for database operations.
    Methods:
    - `saveCondition(labelName, ast, projectId, userId)`:
        1. Generate hash using `ConditionHasher`.
        2. Check `conditions` table for existing hash.
        3. If missing, insert into `conditions` (id = hash, definition = ast).
        4. Upsert into `condition_labels` (name = labelName, condition_hash = hash, project_id = projectId).
    - `getConditionByLabel(labelName, projectId)`: Retrieve the structural definition via the label mapping.
    - `getConditionByHash(hash)`: Direct retrieval from the structural table.
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/condition-engine/ConditionRepository.test.ts</automated>
  </verify>
  <done>Repository correctly implements structural deduplication and label-to-hash mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Evaluation Context Builder</name>
  <files>modular-monolith/src/modules/autopilot/condition-engine/ContextBuilder.ts</files>
  <action>
    Implement `buildEvaluationContext(entityType, entityId, wasSnapshot)`:
    1. Fetch the latest state of the entity from the database (`is` state).
    2. Construct the `EvaluationContext` object with the fresh `is` state and the provided `was` snapshot.
    Handle cases where the entity might have been deleted (empty `is`).
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/condition-engine/ContextBuilder.test.ts</automated>
  </verify>
  <done>Context builder fetches fresh DB state and merges with snapshots correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Database | SQL injection via Kysely (mitigated by parameterized queries). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Elevation of Privilege | Repository | mitigate | Ensure `project_id` scoping is enforced on all label operations. |
| T-23-04 | Tampering | ContextBuilder | mitigate | Ensure state fetching is read-only and respects user visibility rules. |
</threat_model>

<verification>
1. `bun run check-types` passes.
2. `bun test` passes for repository and context builder.
3. Integration test: Save a condition, fetch it, build context, and evaluate.
4. `graphify update .` and `bun run format`.
</verification>

<success_criteria>
- Structural deduplication (COND-06) verified: identical logic only exists once in `conditions`.
- Named conditions (COND-01) work via `condition_labels`.
- `EvaluationContext` correctly provides fresh `is` and historical `was` state.
</success_criteria>

<output>
Create `.planning/phases/23-condition-engine/23-02-SUMMARY.md` when done
</output>
