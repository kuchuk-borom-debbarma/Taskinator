---
phase: 25-pipeline-orchestrator
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.ts
  - modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.test.ts
autonomous: true
requirements: [PIPE-01, PIPE-02]
must_haves:
  truths:
    - "EventListener consumes PIPELINE.TRIGGER and runs first step"
    - "EventListener consumes PIPELINE.CONTINUE and runs subsequent step"
    - "Outbox is used to emit next step for atomic resumability"
  artifacts:
    - path: "modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.ts"
      provides: "Kafka event handling"
---

<objective>
Enable event-driven, resumable pipeline execution by implementing the PipelineEventListener.
This ensures that long pipelines do not block transactions and can scale across the 10k RPS architecture.

Purpose: Decouple step execution and provide fault tolerance via Kafka.
Output: PipelineEventListener with Kafka integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-pipeline-orchestrator/RESEARCH.md
@modular-monolith/src/modules/autopilot/orchestrator/PipelineOrchestrator.ts
@modular-monolith/src/utils/event-bus/OutboxQueries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PipelineEventListener</name>
  <files>modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.ts</files>
  <action>
    Create `PipelineEventListener` class.
    Implement `onTrigger(event)`: Initial execution, generates `traceId`, sets `depth: 0`, `stepIndex: 0`.
    Implement `onContinue(event)`: Resumed execution, uses `traceId`, `depth`, and `stepIndex` from payload.
    Call `PipelineOrchestrator.executeStep` for both.
  </action>
  <verify>
    <automated>bun run format && npx tsc --noEmit</automated>
  </verify>
  <done>EventListener skeleton and handler logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Outbox Continuation Logic</name>
  <files>modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.ts</files>
  <action>
    Integrate `appendEventsToOutbox` in the event handler.
    If `executeStep` returns a result with `nextIndex`, append a `PIPELINE.CONTINUE` event to the outbox within the same transaction.
    Ensure `traceId` and `depth` are propagated correctly.
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.test.ts</automated>
  </verify>
  <done>Pipelines automatically continue to the next step via Kafka events.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Loop Safety Propagation</name>
  <files>modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.ts, modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.test.ts</files>
  <action>
    Add `isRecursiveTrigger` flag to the event payload.
    If an action emits a NEW `PIPELINE.TRIGGER`, set `isRecursiveTrigger: true`.
    In `PipelineEventListener`, if `isRecursiveTrigger` is true, increment `depth` before calling `executeStep`.
    This satisfies the feedback: increment only on recursive triggers, not horizontal steps.
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.test.ts && cd modular-monolith && bun run format && cd .. && node "$HOME/.gemini/get-shit-done/bin/gsd-tools.cjs" graphify update .</automated>
  </verify>
  <done>Depth counter correctly tracks recursion across event boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka → EventListener | Untrusted payloads from the wire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Tampering | Event Payload | mitigate | Verify `autopilotId` existence and ownership before execution. |
| T-25-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
1. `PipelineEventListener.test.ts` passes.
2. `bun run format` executed.
3. `graphify update .` executed.
</verification>

<success_criteria>
- EventListener successfully handles a chain of 3 steps across 3 Kafka events.
- Recursive triggers increment depth.
- Continuation triggers do NOT increment depth.
</success_criteria>

<output>
Create `.planning/phases/25-pipeline-orchestrator/25-02-SUMMARY.md` when done
</output>
