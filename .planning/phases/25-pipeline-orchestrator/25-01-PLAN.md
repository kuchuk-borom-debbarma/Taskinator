---
phase: 25-pipeline-orchestrator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modular-monolith/src/modules/autopilot/orchestrator/PipelineOrchestrator.ts
  - modular-monolith/src/modules/autopilot/orchestrator/types.ts
  - modular-monolith/src/modules/autopilot/orchestrator/PipelineOrchestrator.test.ts
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03]
must_haves:
  truths:
    - "Orchestrator executes steps in correct order"
    - "Orchestrator halts on false condition"
    - "Orchestrator stops if depth exceeds 50"
  artifacts:
    - path: "modular-monolith/src/modules/autopilot/orchestrator/PipelineOrchestrator.ts"
      provides: "Core execution logic"
    - path: "modular-monolith/src/modules/autopilot/orchestrator/types.ts"
      provides: "Execution context types"
  key_links:
    - from: "PipelineOrchestrator.ts"
      to: "ConditionEvaluator.ts"
      via: "evaluateCondition call"
    - from: "PipelineOrchestrator.ts"
      to: "ActionExecutor.ts"
      via: "executeAction call"
---

<objective>
Implement the core Pipeline Orchestrator logic to handle sequential step execution (Conditions/Actions), strict halt semantics, and loop detection.

Purpose: Provide the brain of the Autopilot engine that knows how to move from one step to the next.
Output: PipelineOrchestrator service with unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-pipeline-orchestrator/RESEARCH.md
@modular-monolith/src/modules/autopilot/condition-engine/ConditionEvaluator.ts
@modular-monolith/src/modules/autopilot/action-engine/ActionExecutor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Orchestrator Types and Skeleton</name>
  <files>modular-monolith/src/modules/autopilot/orchestrator/types.ts, modular-monolith/src/modules/autopilot/orchestrator/PipelineOrchestrator.ts</files>
  <action>
    Create `types.ts` defining `PipelineStep` (type: 'condition'|'action', refId: string), `ExecutionState` (traceId, depth, stepIndex, wasSnapshot), and `ExecutionResult`.
    Create `PipelineOrchestrator` class skeleton.
    Inject `ConditionEvaluator`, `ActionExecutor`, `ConditionRepository`, and `ActionRepository`.
  </action>
  <verify>
    <automated>bun run format && npx tsc --noEmit</automated>
  </verify>
  <done>Types defined and class skeleton compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Step Execution and Halt Logic</name>
  <files>modular-monolith/src/modules/autopilot/orchestrator/PipelineOrchestrator.ts</files>
  <action>
    Implement `executeStep(autopilotId, state)` method:
    1. Fetch Autopilot steps from DB/Repository.
    2. Get current step at `state.stepIndex`.
    3. If 'condition': call `evaluateCondition`. If `false`, return `{ status: 'HALTED', reason: 'CONDITION_FAILED' }`.
    4. If 'action': call `executeAction`. Capture the updated state.
    5. Return result with `nextIndex` if more steps remain.
    Ensure strict halt per PIPE-03.
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/orchestrator/PipelineOrchestrator.test.ts</automated>
  </verify>
  <done>Orchestrator correctly handles condition halts and action execution.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Loop Safety (Depth Tracking)</name>
  <files>modular-monolith/src/modules/autopilot/orchestrator/PipelineOrchestrator.ts, modular-monolith/src/modules/autopilot/orchestrator/PipelineOrchestrator.test.ts</files>
  <action>
    Implement `checkLoopSafety(state)`:
    - If `state.depth > 50`, throw `LoopDetectionError` or return `HALTED`.
    - Ensure `depth` is passed through but NOT incremented for horizontal steps.
    - Write tests confirming halt at depth 51.
    Per prompt feedback: depth increments ONLY on recursive triggers (not yet implemented in 01, but the check belongs here).
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/orchestrator/PipelineOrchestrator.test.ts && cd modular-monolith && node -v && bun run format && cd .. && node "$HOME/.gemini/get-shit-done/bin/gsd-tools.cjs" graphify update .</automated>
  </verify>
  <done>Loop detection prevents infinite recursion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Autopilot Config → Orchestrator | Untrusted JSONB `steps` from DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Denial of Service | Pipeline Loop | mitigate | Depth counter max 50 prevents infinite execution chains. |
| T-25-02 | Information Disclosure | Snapshots | mitigate | `wasSnapshot` in execution context must not include sensitive fields (PII). |
</threat_model>

<verification>
1. All unit tests in `PipelineOrchestrator.test.ts` pass.
2. `bun run format` executed.
3. `graphify update .` executed.
</verification>

<success_criteria>
- Orchestrator executes a 3-step pipeline (C-A-C) correctly.
- Orchestrator stops at step 2 if condition fails.
- Orchestrator halts if simulated depth is 51.
</success_criteria>

<output>
Create `.planning/phases/25-pipeline-orchestrator/25-01-SUMMARY.md` when done
</output>
