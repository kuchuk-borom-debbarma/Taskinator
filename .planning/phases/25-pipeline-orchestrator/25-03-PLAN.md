---
phase: 25-pipeline-orchestrator
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
  - modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts
  - modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.ts
autonomous: true
requirements: [PIPE-01, PIPE-02]
must_haves:
  truths:
    - "Aggregator buffers multiple action results"
    - "Aggregator flushes on 100ms timer or batch size"
    - "Aggregator performs bulk updates using SQL CASE statements"
  artifacts:
    - path: "modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts"
      provides: "Bulk update optimization"
---

<objective>
Implement the Smart Aggregator to optimize high-throughput action execution.
By buffering changes and flushing them in optimized bulk SQL queries, we minimize DB pressure and support 10k RPS targets.

Purpose: Performance optimization for large-scale automation.
Output: SmartAggregator service integrated with the Pipeline loop.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-pipeline-orchestrator/RESEARCH.md
@modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SmartAggregator Buffer and Flush</name>
  <files>modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts</files>
  <action>
    Create `SmartAggregator` class.
    Implement an in-memory `AggregationBuffer` grouped by `entityType`.
    Implement `push(entityType, entityId, changes, traceId)`.
    Implement a `flush()` method triggered by a 100ms `setTimeout` (re-scheduled on push) or a max batch size (e.g., 500 items).
  </action>
  <verify>
    <automated>bun run format && npx tsc --noEmit</automated>
  </verify>
  <done>Buffering and timer-based flush logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Bulk Update Logic (CASE Statements)</name>
  <files>modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts, modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts</files>
  <action>
    In `flush()`, for each `entityType`:
    1. Collect all pending changes for all IDs.
    2. Construct a single Kysely `UPDATE` query using `sql` template literal to build CASE statements.
    Pattern: `SET field = CASE id WHEN val1 THEN new_val1 ... ELSE field END`.
    Ensure the query updates multiple fields and multiple rows in one roundtrip.
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts</automated>
  </verify>
  <done>Aggregator successfully flushes heterogeneous updates in single queries.</done>
</task>

<task type="auto">
  <name>Task 3: Wire EventListener to SmartAggregator</name>
  <files>modular-monolith/src/modules/autopilot/orchestrator/PipelineEventListener.ts, modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts</files>
  <action>
    Inject `SmartAggregator` into `PipelineEventListener`.
    After a successful Action step, instead of immediate DB update (if applicable), push the changes to `SmartAggregator`.
    Ensure the `PIPELINE.CONTINUE` event is still emitted correctly.
    Note: For immediate visibility requirements, the orchestrator might skip aggregator; for 10k RPS background tasks, it uses aggregator. Implement as a flag in the action result.
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts && cd modular-monolith && bun run format && cd .. && node "$HOME/.gemini/get-shit-done/bin/gsd-tools.cjs" graphify update .</automated>
  </verify>
  <done>Pipeline execution flow is now optimized via the Smart Aggregator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aggregator → Database | Trusted component performing bulk writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Data Integrity | Bulk Update | mitigate | Ensure the WHERE clause of bulk updates strictly matches the aggregated ID set. |
| T-25-05 | Availability | Memory Exhaustion | mitigate | Max buffer size (e.g. 10,000 items) triggers immediate flush. |
</threat_model>

<verification>
1. All unit tests pass.
2. `bun run format` executed.
3. `graphify update .` executed.
</verification>

<success_criteria>
- 100 concurrent actions result in < 5 database update queries.
- Bulk updates correctly apply different values to different fields across rows.
- Timer flushes pending changes even if batch size not reached.
</success_criteria>

<output>
Create `.planning/phases/25-pipeline-orchestrator/25-03-SUMMARY.md` when done
</output>
