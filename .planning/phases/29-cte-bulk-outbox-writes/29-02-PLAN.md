---
phase: 29-cte-bulk-outbox-writes
plan: "02"
type: execute
wave: 2
depends_on: [29-01]
files_modified: [
  modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts,
  modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts
]
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03]
must_haves:
  truths:
    - "Push method strictly validates presence of traceId and depth"
    - "Failed buffer merging recovery preserves the oldest original traceId and depth"
    - "Aggregator test suite successfully covers new validations, recovery semantics, and dynamic CTE generation"
  artifacts:
    - path: "modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts"
      provides: "Aggregator validation and CTE compiler unit tests"
---

<objective>
Integrate strict header validation in push, old-trace buffer recovery, and extend the aggregator test suites.

Purpose: Prevent untraced pipeline state writes, protect loop safety bounds, and ensure robust production-ready test coverage.
Output: Integrated safety gates and expanded test suites.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/29-cte-bulk-outbox-writes/29-CONTEXT.md
@.planning/phases/29-cte-bulk-outbox-writes/29-RESEARCH.md
@modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
@modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Strict Validation in SmartAggregator.push()</name>
  <files>
    modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
  </files>
  <read_first>
    modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
  </read_first>
  <behavior>
    - Raise an explicit Error if `traceId` is missing/empty.
    - Raise an explicit Error if `depth` is null, undefined, or not a number.
    - Output clear, descriptive logs capturing the table and entity ID on failure.
  </behavior>
  <action>
    Inject validation checks at the very start of the `push()` method in `SmartAggregator.ts` to enforce the existence of `traceId` and `depth`.
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts</automated>
  </verify>
  <done>SmartAggregator.push throws explicit exceptions on untraced mutations</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix Buffer Recovery Merging to Keep Oldest Trace</name>
  <files>
    modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
  </files>
  <read_first>
    modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
  </read_first>
  <behavior>
    - When failed flush items are restored and merged into the buffer, the oldest original `traceId` and `depth` (i.e. `item.traceId` and `item.depth`) must be retained instead of adopting the newer `existing` parameters.
  </behavior>
  <action>
    Modify the merge assignment in `SmartAggregator.ts` flush catch recovery block to map `traceId: item.traceId` and `depth: item.depth`.
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts</automated>
  </verify>
  <done>Buffer recovery merging logic correctly preserves oldest trace identifiers</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend Unit Test Coverage in SmartAggregator.test.ts</name>
  <files>
    modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts
  </files>
  <read_first>
    modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts,
    modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
  </read_first>
  <behavior>
    - Add tests asserting that `push()` throws when `traceId` or `depth` are missing/invalid.
    - Add tests asserting that failed flush recovery retains the oldest traceId/depth.
    - Add tests asserting that dynamic CTE queries are compiled and execute correctly for `project_team` and `project` entity types.
  </behavior>
  <action>
    Update `SmartAggregator.test.ts` to implement the new unit test assertions, ensuring 100% code coverage on the dynamic compiler write paths.
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts</automated>
  </verify>
  <done>SmartAggregator.test.ts fully covers new validations, merging rules, and CTE types</done>
</task>

</tasks>

<success_criteria>
SmartAggregator test suite successfully runs and covers validation constraints, recovery merges, and multiple dynamic table CTE compilations with zero failures.
</success_criteria>

<output>
Create `.planning/phases/29-cte-bulk-outbox-writes/29-02-SUMMARY.md` when done
</output>
