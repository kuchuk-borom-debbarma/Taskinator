---
phase: 29-cte-bulk-outbox-writes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
]
autonomous: true
requirements: [CTE-01, CTE-02, CTE-03]
must_haves:
  truths:
    - "SmartAggregator compiles table-agnostic CTE queries programmatically"
    - "Dynamically formats old/new JSON payloads based on registered entity schemas"
  artifacts:
    - path: "modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts"
      provides: "Dynamic PostgreSQL CTE query compiler"
---

<objective>
Upgrade SmartAggregator to dynamically compile Kysely CTE transactional outbox queries for multiple table types.

Purpose: Enable transactional single-query outbox event generation under high throughput (10k RPS target) for multiple autopilot automation entities.
Output: Programmatic PostgreSQL CTE builder inside SmartAggregator.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/29-cte-bulk-outbox-writes/29-CONTEXT.md
@.planning/phases/29-cte-bulk-outbox-writes/29-RESEARCH.md
@modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Entity Metadata Registry</name>
  <files>
    modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
  </files>
  <read_first>
    modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts,
    modular-monolith/src/database/tables/ProjectTask.ts,
    modular-monolith/src/database/tables/ProjectTeam.ts,
    modular-monolith/src/database/tables/Project.ts
  </read_first>
  <behavior>
    - Declare a metadata map `ENTITY_CONFIGS` registry inside `SmartAggregator.ts` mapped to `project_task`, `project_team`, and `project` schemas.
    - Provide topic, eventType, selectFields, keyField, idField, and payloadFields mapping structures.
  </behavior>
  <action>
    Define the `EntityConfig` interface and add the `ENTITY_CONFIGS` object to `SmartAggregator.ts` with configurations for:
    - `project_task` (topic: 'task-events', event: 'task.updated', key: 'fk_project_id', idField: 'taskId')
    - `project_team` (topic: 'team-events', event: 'team.updated', key: 'fk_project_id', idField: 'teamId')
    - `project` (topic: 'project-events', event: 'project.updated', key: 'id', idField: 'projectId')
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts</automated>
  </verify>
  <done>Entity config registry is defined and populated in SmartAggregator.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor executeBulkUpdate for Dynamic CTE Generation</name>
  <files>
    modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
  </files>
  <read_first>
    modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.ts
  </read_first>
  <behavior>
    - Query matches entityType in `ENTITY_CONFIGS`. If found, build the dynamic CTE query using programmatically generated CASE statements for `traceIdCase`, `depthCase`, and dynamic `jsonb_build_object` old/new state builders.
    - Execute the complete CTE combining `old_state`, `updated_entity`, and `inserted_outbox` in a single transactional query.
    - If `entityType` is not found, fallback to simple `UPDATE` query.
  </behavior>
  <action>
    Rewrite `executeBulkUpdate` in `SmartAggregator.ts` to:
    - Match `ENTITY_CONFIGS[entityType]`.
    - Build `traceIdCase` and `depthCase` dynamically.
    - Build dynamic `jsonb_build_object` pairs for old state and new state using `config.payloadFields`.
    - Compile and execute the full `WITH old_state AS...` raw SQL.
  </action>
  <verify>
    <automated>bun test modular-monolith/src/modules/autopilot/orchestrator/SmartAggregator.test.ts</automated>
  </verify>
  <done>SmartAggregator dynamically compiles and executes table-agnostic CTE updates</done>
</task>

</tasks>

<success_criteria>
executeBulkUpdate correctly processes registered entity tables with high-performance Postgres CTE transactions and programmatically structured JSON outbox payloads.
</success_criteria>

<output>
Create `.planning/phases/29-cte-bulk-outbox-writes/29-01-SUMMARY.md` when done
</output>
