---
phase: 15-refactoring-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - remotion/src/SchemaDesign.tsx
  - remotion/src/QueryProblem.tsx
  - remotion/src/DenormalizationSolution.tsx
  - remotion/src/DenormalizationDrawback.tsx
  - remotion/src/TaskLinkProblem.tsx
  - remotion/src/ClosureTableSolution.tsx
  - remotion/src/ClosureTableDrawback.tsx
autonomous: true
requirements: [REFACTOR-02]

must_haves:
  truths:
    - "SchemaDesign.tsx defines premountFor={1 * fps} on all top-level Sequence wrappers"
    - "All 6 sub-scene files use explicit clamping configurations in all interpolate calls"
  artifacts:
    - path: "remotion/src/SchemaDesign.tsx"
      provides: "Master schema design layout"
    - path: "remotion/src/QueryProblem.tsx"
      provides: "Read amplification scene"
---

<objective>
Refactor SchemaDesign master component and all 6 nested sub-scenes to obey strict Remotion timing and sequencing rules.

Purpose: Maximize scene transitions fluidity and guarantee error-free clamp states for all motion animations.
Output: Seven fully refactored React/Remotion source files.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-refactoring-baseline/15-RESEARCH.md
@remotion/src/SchemaDesign.tsx
@remotion/src/QueryProblem.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor SchemaDesign.tsx Sequences and SVG connections</name>
  <files>remotion/src/SchemaDesign.tsx</files>
  <read_first>remotion/src/SchemaDesign.tsx, .planning/phases/15-refactoring-baseline/15-RESEARCH.md</read_first>
  <action>1. Import `Easing` from "remotion". 
2. Locate all Sequence blocks inside the Root Composition (lines 336-373) and inside SchemaScene (lines 309-319). Add `premountFor={1 * fps}` and `layout="none"` to each.
3. In FkLine (line 167), the `spring` computes the `progress` animation. While springs are fine for physics-based connectors, review the `interpolate` logic to make sure it uses clamped options if any exist.</action>
  <verify>grep "premountFor" remotion/src/SchemaDesign.tsx</verify>
  <acceptance_criteria>
    - SchemaDesign.tsx contains "premountFor" on sequences
  </acceptance_criteria>
  <done>SchemaDesign.tsx sequences fully optimized.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor QueryProblem and remaining sub-scenes to clamp all animations</name>
  <files>remotion/src/QueryProblem.tsx, remotion/src/DenormalizationSolution.tsx, remotion/src/DenormalizationDrawback.tsx, remotion/src/TaskLinkProblem.tsx, remotion/src/ClosureTableSolution.tsx, remotion/src/ClosureTableDrawback.tsx</files>
  <read_first>remotion/src/QueryProblem.tsx, remotion/src/DenormalizationSolution.tsx, remotion/src/DenormalizationDrawback.tsx, remotion/src/TaskLinkProblem.tsx, remotion/src/ClosureTableSolution.tsx, remotion/src/ClosureTableDrawback.tsx</read_first>
  <action>Scan all six sub-scene source files. For every file:
1. Import `Easing` from "remotion".
2. Locate all occurrences of `interpolate(val, [0, 1], ...)` or other linear mappings.
3. Add the mandatory fourth parameter `{ extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: Easing.bezier(0.16, 1, 0.3, 1) }` or `Easing.out(Easing.cubic)` to every `interpolate` call.
4. Check for any `<Sequence>` wrappers inside these subcomponents and ensure they have `premountFor={1 * fps}` set.</action>
  <verify>grep -l "extrapolateLeft" remotion/src/QueryProblem.tsx remotion/src/DenormalizationSolution.tsx</verify>
  <acceptance_criteria>
    - All six sub-scene files contain "extrapolateLeft"
  </acceptance_criteria>
  <done>All sub-scenes successfully migrated to clamped ease curves.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify files compile seamlessly.
- [ ] Check that animation flow remains visually coherent.
</verification>

<success_criteria>
- All tasks completed
- Standardized clamp interpolation applies to entire Schema design act.
</success_criteria>

<output>
After completion, create `.planning/phases/15-refactoring-baseline/15-02-SUMMARY.md`
</output>
