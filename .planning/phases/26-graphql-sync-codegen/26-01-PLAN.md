---
phase: "26-graphql-sync-codegen"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "modular-monolith/src/graphql/schema/autopilot/autopilot.graphql"
  - "modular-monolith/src/graphql/resolvers/autopilot.ts"
  - "modular-monolith/schema.graphql"
autonomous: true
requirements: ["GQL-01"]
must_haves:
  truths:
    - "Schema includes PipelineStep union, structured ConditionNode unions, and updated inputs"
    - "Autopilot.pipeline resolver hydrations logic from DB hashes"
    - "autopilotMetadata returns valid field/operator sets per entity"
    - "Root schema.graphql is synchronized with individual module schemas"
  artifacts:
    - path: "modular-monolith/src/graphql/schema/autopilot/autopilot.graphql"
      provides: "Unified pipeline and recursive AST schema"
    - path: "modular-monolith/src/graphql/resolvers/autopilot.ts"
      provides: "Hydrated resolvers with recursive AST support"
    - path: "modular-monolith/schema.graphql"
      provides: "Synchronized root schema"
---

<objective>
Align backend GraphQL with v6.0 engine primitives. Implement recursive AST hydration, pipeline hydration, and metadata discovery. Synchronize root schema.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/26-graphql-sync-codegen/26-CONTEXT.md
@.planning/phases/26-graphql-sync-codegen/RESEARCH.md

<interfaces>
From modular-monolith/src/modules/autopilot/condition-engine/ConditionRepository.ts:
```typescript
export interface ConditionAST {
    type: 'and' | 'or' | 'not' | 'predicate';
    children?: ConditionAST[];
    child?: ConditionAST;
    domain?: string;
    field?: string;
    operator?: string;
    value?: any;
}

export class ConditionRepository {
    async getConditionByHash(hash: string): Promise<ConditionAST | null>;
}
```

From modular-monolith/src/modules/autopilot/action-engine/ActionRepository.ts:
```typescript
export interface ActionAST {
    type: string;
    params: Record<string, any>;
}

export class ActionRepository {
    async getActionByHash(hash: string): Promise<ActionAST | null>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Autopilot GraphQL Schema (Unions & Mutations)</name>
  <files>modular-monolith/src/graphql/schema/autopilot/autopilot.graphql</files>
  <action>
    Define `PipelineStep` union of `AutopilotCondition` and `AutopilotAction` per RESEARCH.md.
    Update `Autopilot` type: remove `conditions`/`actions`, add `pipeline: [PipelineStep!]!`.
    Implement structured recursive `ConditionNode` union (AndNode, OrNode, NotNode, PredicateNode) to replace scalar/string definitions.
    Align `CreateAutopilotInput` and `UpdateAutopilotInput` to use unified `pipeline` input structure.
    Add `AutopilotMetadata` types and `Query.autopilotMetadata(entityType: String!)`.
  </action>
  <verify>
    <automated>grep -q "union PipelineStep" modular-monolith/src/graphql/schema/autopilot/autopilot.graphql && grep -q "union ConditionNode" modular-monolith/src/graphql/schema/autopilot/autopilot.graphql</automated>
  </verify>
  <done>Schema defines pipeline union, recursive AST nodes, and aligned mutations</done>
</task>

<task type="auto">
  <name>Task 2: Implement Recursive Resolvers & Hydration</name>
  <files>modular-monolith/src/graphql/resolvers/autopilot.ts</files>
  <action>
    Update `Autopilot` resolver to handle the `pipeline` field. 
    Map stored step hashes to full objects using `ConditionRepository.getConditionByHash` and `ActionRepository.getActionByHash` per D-02.
    Implement recursive resolvers for `ConditionNode` (AndNode, OrNode, NotNode) to handle nested `ConditionAST` structures.
    Ensure `__typename` is correctly assigned for all Union types (PipelineStep and ConditionNode).
    Implement `Query.autopilotMetadata` resolver returning entity-specific predicate/action configs per D-03.
  </action>
  <verify>
    <automated>cd modular-monolith && npm run build</automated>
  </verify>
  <done>Resolvers hydrate pipeline and recursive AST structures correctly</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize Root Schema</name>
  <files>modular-monolith/schema.graphql</files>
  <action>
    Run the schema generation command in `modular-monolith` to merge the updated autopilot schema into the root `schema.graphql`.
    Command: `bun run generate-schema` (as per RESEARCH.md).
  </action>
  <verify>
    <automated>grep -q "union PipelineStep" modular-monolith/schema.graphql</automated>
  </verify>
  <done>Root schema.graphql is synchronized with module changes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Metadata query allows discovery of system fields |
| API -> DB | Resolver hydration uses hashes from DB; must ensure repository lookups are safe |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | autopilotMetadata | mitigate | Filter metadata by user permissions/project visibility |
| T-26-02 | Denial of Service | Recursive AST | mitigate | Limit maximum depth of ConditionNode recursion in resolvers |
| T-26-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<success_criteria>
Backend GraphQL schema and resolvers aligned with v6.0 engine, root schema synchronized.
</success_criteria>

<output>
Create `.planning/phases/26-graphql-sync-codegen/26-01-SUMMARY.md` when done
</output>
