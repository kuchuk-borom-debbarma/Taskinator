---
phase: "26-graphql-sync-codegen"
plan: "02"
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - "ui-v1/src/gql/graphql.ts"
  - "ui-v1/src/components/Autopilot/AutopilotDashboardView.tsx"
  - "ui-v1/src/components/Autopilot/AutopilotList.tsx"
  - "ui-v1/src/components/Autopilot/AutopilotCard.tsx"
  - "ui-v1/src/components/Autopilot/CreateAutopilotModal.tsx"
  - "ui-v1/src/api/adapters/graphql/GraphQLAutopilotAPI.ts"
autonomous: true
requirements: ["GQL-02"]
must_haves:
  truths:
    - "ui-v1 codegen output includes PipelineStep, ConditionNode, and autopilotMetadata types"
    - "AutopilotDashboardView and AutopilotList use updated GraphQL fragments and hooks"
    - "CreateAutopilotModal refactored to use new unified pipeline mutation input"
    - "Manual GraphQLAutopilotAPI adapter is removed"
  artifacts:
    - path: "ui-v1/src/gql/graphql.ts"
      provides: "Updated GQL types with fragments"
    - path: "ui-v1/src/api/adapters/graphql/GraphQLAutopilotAPI.ts"
      provides: "DELETED"
---

<objective>
Synchronize ui-v1 with backend schema changes via codegen. Fix all component breakages, refactor mutation logic, and remove deprecated API adapters.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/26-graphql-sync-codegen/26-CONTEXT.md
@.planning/phases/26-graphql-sync-codegen/26-01-SUMMARY.md

# Updated schema from backend for codegen reference
@modular-monolith/schema.graphql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute Codegen & Fragments</name>
  <files>ui-v1/src/gql/graphql.ts</files>
  <action>
    Run `npm run codegen` in `ui-v1` directory to pull updated schema from backend.
    Ensure `PipelineStep` union, `ConditionNode` union, and `autopilotMetadata` types are generated.
    Verify fragment masking is working (if enabled in codegen config).
  </action>
  <verify>
    <automated>grep -q "PipelineStep" ui-v1/src/gql/graphql.ts && grep -q "ConditionNode" ui-v1/src/gql/graphql.ts</automated>
  </verify>
  <done>Codegen output reflects backend schema changes including recursive AST nodes</done>
</task>

<task type="auto">
  <name>Task 2: Fix Dashboard & List Components</name>
  <files>ui-v1/src/components/Autopilot/AutopilotDashboardView.tsx, ui-v1/src/components/Autopilot/AutopilotList.tsx, ui-v1/src/components/Autopilot/AutopilotCard.tsx</files>
  <action>
    Update GraphQL queries in `AutopilotDashboardView` and `AutopilotList` to include fragments for the `PipelineStep` union.
    Fix `AutopilotCard` and other list-view components to consume the `pipeline` array instead of separate conditions/actions.
    Refactor rendering logic to handle the mixed array of `PipelineStep` objects.
  </action>
  <verify>
    <automated>cd ui-v1 && npm run build</automated>
  </verify>
  <done>Dashboard and List components correctly render the new pipeline structure</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Mutation Logic & Deprecate Adapter</name>
  <files>ui-v1/src/components/Autopilot/CreateAutopilotModal.tsx, ui-v1/src/api/adapters/graphql/GraphQLAutopilotAPI.ts</files>
  <action>
    Refactor form submission in `CreateAutopilotModal` to use the new `CreateAutopilotInput` structure (unified pipeline array).
    Migrate from the manual `GraphQLAutopilotAPI` adapter to the generated `useMutation` hooks from codegen.
    Update payload mapping to handle the structured AST for conditions.
    Explicitly delete `ui-v1/src/api/adapters/graphql/GraphQLAutopilotAPI.ts`.
  </action>
  <verify>
    <automated>ls ui-v1/src/api/adapters/graphql/GraphQLAutopilotAPI.ts 2>/dev/null || echo "Deleted"</automated>
  </verify>
  <done>Mutation logic refactored to use codegen hooks; legacy adapter removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GQL Codegen | Automated trust boundary for type safety |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering | Mutation payload | mitigate | Ensure frontend validation matches backend schema constraints before submission |
| T-26-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint if new packages added |
</threat_model>

<success_criteria>
ui-v1 synchronized with backend, components functional with the new pipeline structure, and legacy code removed.
</success_criteria>

<output>
Create `.planning/phases/26-graphql-sync-codegen/26-02-SUMMARY.md` when done
</output>
