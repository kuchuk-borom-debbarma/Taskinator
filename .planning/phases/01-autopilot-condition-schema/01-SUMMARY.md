# Phase 01 Summary: Foundation & Condition Schema
- Defined `ConditionNode` and `Predicate` types.
- Created `autopilot` table with `triggers` and `conditions`.
- Implemented basic validation for condition trees.
