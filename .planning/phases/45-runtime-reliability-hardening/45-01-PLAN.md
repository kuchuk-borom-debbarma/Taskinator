---
phase: 45-runtime-reliability-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - modular-monolith/src/auto-action/service.ts
  - modular-monolith/src/auto-action/consumer.ts
  - modular-monolith/src/tests/auto-action-idempotency.test.ts
autonomous: true
requirements: 
  - REL-01
  - REL-02
must_haves:
  truths:
    - Logs contain correlation IDs for all auto-actions.
    - Idempotency test fails for duplicate events.
    - Idempotency test passes after logic implemented.
  artifacts:
    - path: modular-monolith/src/tests/auto-action-idempotency.test.ts
      provides: "Idempotency validation test case"
    - path: modular-monolith/src/auto-action/service.ts
      provides: "Idempotency check logic"
  key_links:
    - from: modular-monolith/src/auto-action/consumer.ts
      to: modular-monolith/src/auto-action/service.ts
      via: "event processing"
---

<objective>
Enhance instrumentation and ensure idempotency for auto-action processing.

Purpose: Prevent duplicate side effects from re-driven events and provide observability.
Output: Instrumentation logs, idempotency validation suite.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@modular-monolith/src/auto-action/consumer.ts
@modular-monolith/src/auto-action/service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add idempotency test suite</name>
  <files>modular-monolith/src/tests/auto-action-idempotency.test.ts</files>
  <behavior>
    - Test 1: Send duplicate event with same ID -> expect service to execute only once.
    - Test 2: Send two distinct events -> expect service to execute twice.
  </behavior>
  <action>Create test suite validating idempotency for auto-actions. Mock event consumer/service interaction. Verify duplicate processing prevention.</action>
  <verify>
    <automated>npm test src/tests/auto-action-idempotency.test.ts</automated>
  </verify>
  <done>Idempotency tests fail as expected (RED phase).</done>
</task>

<task type="auto">
  <name>Task 2: Implement idempotency and instrumentation</name>
  <files>modular-monolith/src/auto-action/service.ts, modular-monolith/src/auto-action/consumer.ts</files>
  <action>Add correlation ID logging in consumer. Implement DB-level idempotency check (unique event ID tracking) in service before execution. Ensure service logic passes tests.</action>
  <verify>
    <automated>npm test src/tests/auto-action-idempotency.test.ts</automated>
  </verify>
  <done>Tests pass (GREEN phase), correlation logs visible in stdout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- {event-bus → consumer}: untrusted event payloads.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | Event Consumer | mitigate | Validate event schema + idempotency key. |
| T-45-02 | Info Disclosure | Logger | accept | Low-sensitivity trace IDs only; no payload logging. |
</threat_model>

<verification>
Automated: npm test src/tests/auto-action-idempotency.test.ts
</verification>

<success_criteria>
- Idempotency test passes (no duplicate processing).
- Execution logs include consistent correlation IDs.
</success_criteria>
