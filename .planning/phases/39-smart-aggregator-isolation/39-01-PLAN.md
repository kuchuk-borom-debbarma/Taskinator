---
phase: 39-smart-aggregator-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
    "modular-monolith/src/utils/event-bus/AggregatorService.ts",
    "modular-monolith/src/utils/event-bus/index.ts",
    "modular-monolith/src/kafka/smart-aggregator-consumer/project/ProjectEvents_BatchAggregator.ts",
    "modular-monolith/src/utils/event-bus/__tests__/AggregatorService.test.ts"
]
autonomous: true
requirements: [R1, R4]
must_haves:
  truths:
    - "AggregatorService orchestrates atomic aggregator lifecycle"
    - "Project aggregator removed from direct DB transaction management"
    - "AggregatorService has comprehensive unit test coverage"
  artifacts:
    - path: "modular-monolith/src/utils/event-bus/AggregatorService.ts"
      provides: "Aggregator processing logic"
    - path: "modular-monolith/src/kafka/smart-aggregator-consumer/project/ProjectEvents_BatchAggregator.ts"
      contains: "aggregatorService.processAggregatorBatch"
    - path: "modular-monolith/src/utils/event-bus/__tests__/AggregatorService.test.ts"
      provides: "Unit tests for AggregatorService"
  key_links:
    - from: "ProjectEvents_BatchAggregator.ts"
      to: "AggregatorService.ts"
      via: "dependency injection or direct call"
---

<objective>
Create the `AggregatorService` utility to encapsulate the claim-process-outbox pattern, refactor the Project aggregator to use it, and ensure the service is fully tested.

Purpose: Decouple high-frequency event folding from database transaction management with verified reliability.
Output: Generic aggregator service, refactored project aggregator, and test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/39-RESEARCH.md
@modular-monolith/src/kafka/smart-aggregator-consumer/project/ProjectEvents_BatchAggregator.ts
@modular-monolith/src/utils/event-bus/idempotency.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AggregatorService</name>
  <files>modular-monolith/src/utils/event-bus/AggregatorService.ts, modular-monolith/src/utils/event-bus/index.ts</files>
  <action>
    Implement AggregatorService in `modular-monolith/src/utils/event-bus/AggregatorService.ts` per RESEARCH.md recommendation.
    - Add `processAggregatorBatch(groupId, events, folder)` method.
    - Method should:
      1. Start `db.transaction()`.
      2. Call `claimEventsAtomic(trx, events, groupId)`.
      3. Return early if no unprocessed events.
      4. Pass unprocessed events to the `folder` callback.
      5. Append results from `folder` to outbox using `appendEventsToOutbox(trx, entries)`.
    - Export AggregatorService from `modular-monolith/src/utils/event-bus/index.ts`.
  </action>
  <verify>
    <automated>test -f modular-monolith/src/utils/event-bus/AggregatorService.ts && grep "processAggregatorBatch" modular-monolith/src/utils/event-bus/AggregatorService.ts</automated>
  </verify>
  <done>AggregatorService implemented and exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AggregatorService Unit Tests</name>
  <files>modular-monolith/src/utils/event-bus/__tests__/AggregatorService.test.ts</files>
  <behavior>
    - Test 1: Should start a transaction and call claimEventsAtomic.
    - Test 2: Should return early if claimEventsAtomic returns no unprocessed events.
    - Test 3: Should call folder callback with unprocessed events and append results to outbox.
    - Test 4: Should rollback transaction if folder or outbox append fails.
  </behavior>
  <action>
    Create unit tests in `modular-monolith/src/utils/event-bus/__tests__/AggregatorService.test.ts`.
    - Mock `db`, `claimEventsAtomic`, and `appendEventsToOutbox`.
    - Use `jest` (or project standard test runner) to verify the internal flow and error handling.
  </action>
  <verify>
    <automated>npm test modular-monolith/src/utils/event-bus/__tests__/AggregatorService.test.ts</automated>
  </verify>
  <done>AggregatorService has 100% path coverage in unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor ProjectEvents_BatchAggregator</name>
  <files>modular-monolith/src/kafka/smart-aggregator-consumer/project/ProjectEvents_BatchAggregator.ts</files>
  <action>
    Refactor `ProjectEvents_BatchAggregator` to use `aggregatorService.processAggregatorBatch`.
    - Instantiate `AggregatorService` (or use a shared instance).
    - Move the folding logic (semantic folding of project states) into a separate private method or pass it as the `folder` callback.
    - Ensure `db` import is removed from the file (per R1).
    - Maintain exact same semantic folding logic as current implementation.
  </action>
  <verify>
    <automated>grep -v "import.*db" modular-monolith/src/kafka/smart-aggregator-consumer/project/ProjectEvents_BatchAggregator.ts | grep "aggregatorService.processAggregatorBatch"</automated>
  </verify>
  <done>Project aggregator refactored, direct DB usage removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka -> Aggregator | Untrusted events from broker are processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | Outbox Entry | mitigate | AggregatorService uses database transactions to ensure atomic outbox writes. |
| T-39-02 | Denial of Service | Aggregator Batch | mitigate | Batch size is limited by Kafka consumer configuration (not modified in this phase). |
</threat_model>

<verification>
Check that project aggregator still functions by running existing Kafka integration tests if available. Verify AggregatorService tests pass.
</verification>

<success_criteria>
AggregatorService exists and Project aggregator uses it with zero direct `db` references. Unit tests verify AggregatorService logic.
</success_criteria>

<output>
Create `.planning/phases/39-smart-aggregator-isolation/39-01-SUMMARY.md` when done
</output>
