---
phase: 39-smart-aggregator-isolation
plan: 03
type: execute
wave: 2
depends_on: ["39-01"]
files_modified: [
    "modular-monolith/src/modules/auth/AuthService.ts",
    "modular-monolith/src/modules/auth/internal/AuthServiceImpl.ts",
    "modular-monolith/src/modules/auth/internal/listeners/ProjectAggregated_ChangeUserProjectCount.ts",
    "modular-monolith/src/modules/auth/internal/__tests__/AuthServiceBatch.test.ts"
]
autonomous: true
requirements: [R3, R4]
must_haves:
  truths:
    - "AuthService handles user project count synchronization"
    - "Auth listener decoupled from direct DB transaction management"
    - "Auth batch sync logic is verified by unit tests"
  artifacts:
    - path: "modular-monolith/src/modules/auth/AuthService.ts"
      contains: "handleUserProjectCountSync"
    - path: "modular-monolith/src/modules/auth/internal/listeners/ProjectAggregated_ChangeUserProjectCount.ts"
      contains: "authService.handleUserProjectCountSync"
    - path: "modular-monolith/src/modules/auth/internal/__tests__/AuthServiceBatch.test.ts"
      provides: "Unit tests for AuthService batch processing"
---

<objective>
Update AuthService to handle aggregated user project counts, refactor the corresponding listener to use the service, and verify the implementation with tests.

Purpose: Enforce service layer isolation for Auth module updates with guaranteed correctness.
Output: Extended AuthService, refactored auth listener, and verification tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/39-RESEARCH.md
@modular-monolith/src/modules/auth/AuthService.ts
@modular-monolith/src/modules/auth/internal/AuthServiceImpl.ts
@modular-monolith/src/modules/auth/internal/listeners/ProjectAggregated_ChangeUserProjectCount.ts
@modular-monolith/src/utils/event-bus/idempotency.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AuthService Interface and Implementation</name>
  <files>modular-monolith/src/modules/auth/AuthService.ts, modular-monolith/src/modules/auth/internal/AuthServiceImpl.ts</files>
  <action>
    Add `handleUserProjectCountSync(events)` to `AuthService` and implement it in `AuthServiceImpl`.
    - Implementation should:
      1. Start `db.transaction()`.
      2. Call `claimEventsAtomic(trx, events, 'auth-project-aggregator-group')`.
      3. Return early if no unprocessed events.
      4. Consolidate deltas per userId.
      5. Perform bulk update using `updateUserProjectCountsBulk(consolidates, trx)`.
    - Ensure `AuthServiceImpl` has access to necessary query functions.
  </action>
  <verify>
    <automated>grep "handleUserProjectCountSync" modular-monolith/src/modules/auth/AuthService.ts && grep "handleUserProjectCountSync" modular-monolith/src/modules/auth/internal/AuthServiceImpl.ts</automated>
  </verify>
  <done>AuthService updated with batch sync method.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AuthService Batch Sync Tests</name>
  <files>modular-monolith/src/modules/auth/internal/__tests__/AuthServiceBatch.test.ts</files>
  <behavior>
    - Test 1: Should call claimEventsAtomic with the correct group ID.
    - Test 2: Should correctly consolidate multiple delta events for the same user.
    - Test 3: Should execute bulk update with the consolidated deltas.
    - Test 4: Should skip updates if no new events are claimed.
  </behavior>
  <action>
    Create unit tests for the new batch method in `AuthServiceBatch.test.ts`.
    - Mock `db` and `claimEventsAtomic`.
    - Mock internal query `updateUserProjectCountsBulk`.
    - Verify deltas are summed correctly before reaching the query layer.
  </action>
  <verify>
    <automated>npm test modular-monolith/src/modules/auth/internal/__tests__/AuthServiceBatch.test.ts</automated>
  </verify>
  <done>AuthService batch method is fully tested.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Auth Aggregated Listener</name>
  <files>modular-monolith/src/modules/auth/internal/listeners/ProjectAggregated_ChangeUserProjectCount.ts</files>
  <action>
    Refactor `ProjectAggregated_ChangeUserProjectCount` to delegate to `authService`.
    - Inject/use `authService` instance.
    - Replace the internal transaction and consolidation logic with a call to `authService.handleUserProjectCountSync(events)`.
    - Remove `import { db }` and `claimEventsAtomic` from the listener.
  </action>
  <verify>
    <automated>grep -v "import.*db" modular-monolith/src/modules/auth/internal/listeners/ProjectAggregated_ChangeUserProjectCount.ts | grep "authService.handleUserProjectCountSync"</automated>
  </verify>
  <done>Auth listener refactored to use service.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka -> Auth Listener | Aggregated signals from project module are processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | Tampering | User Table | mitigate | AuthService uses transactions and idempotency checks for all updates. |
</threat_model>

<verification>
Ensure existing auth tests pass and `projects_count` correctly updates for users after simulated project events. Verify new batch sync tests pass.
</verification>

<success_criteria>
Auth listener has zero direct `db` references and delegates to AuthService. New service method is verified by unit tests.
</success_criteria>

<output>
Create `.planning/phases/39-smart-aggregator-isolation/39-03-SUMMARY.md` when done
</output>
