---
phase: 39-smart-aggregator-isolation
plan: 05
type: execute
wave: 2
depends_on: ["39-01"]
files_modified: [
    "modular-monolith/src/modules/team/TeamService.ts",
    "modular-monolith/src/modules/team/internal/TeamServiceImpl.ts",
    "modular-monolith/src/modules/team/internal/listeners/TeamAggregated_SyncTeamMemberCountListener.ts",
    "modular-monolith/src/modules/team/internal/listeners/ProjectAggregated_RemoveProjectTeamMember.ts",
    "modular-monolith/src/modules/team/internal/listeners/ProjectAggregated_DeleteProjectTeamMember.ts",
    "modular-monolith/src/modules/team/internal/listeners/ProjectAggregated_DeleteProjectTeam.ts",
    "modular-monolith/src/modules/team/internal/listeners/TeamAggregated_PurgeTeamMembershipsListener.ts",
    "modular-monolith/src/modules/team/internal/listeners/TaskAggregated_SyncTeamTaskCountListener.ts",
    "modular-monolith/src/modules/team/internal/__tests__/TeamServiceBatch.test.ts"
]
autonomous: true
requirements: [R3, R4]
must_haves:
  truths:
    - "TeamService handles aggregated member/task count synchronization"
    - "Team listeners decoupled from direct DB transaction management"
    - "All TeamService batch methods are verified by unit tests"
  artifacts:
    - path: "modular-monolith/src/modules/team/TeamService.ts"
      contains: ["handleSyncTeamMemberCount", "handleRemoveProjectTeamMember", "handleDeleteProjectTeamMember", "handleDeleteProjectTeam", "handlePurgeTeamMemberships", "handleSyncTeamTaskCount"]
    - path: "modular-monolith/src/modules/team/internal/listeners/TeamAggregated_SyncTeamMemberCountListener.ts"
      contains: "teamService.handleSyncTeamMemberCount"
    - path: "modular-monolith/src/modules/team/internal/__tests__/TeamServiceBatch.test.ts"
      provides: "Unit tests for TeamService batch processing"
---

<objective>
Update TeamService with bulk processing methods, refactor team module aggregated listeners, and verify the implementation with comprehensive tests.

Purpose: Enforce service layer isolation for Team module with guaranteed correctness.
Output: Extended TeamService, refactored team listeners, and verification tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/39-RESEARCH.md
@modular-monolith/src/modules/team/TeamService.ts
@modular-monolith/src/modules/team/internal/TeamServiceImpl.ts
@modular-monolith/src/modules/team/internal/listeners/TeamAggregated_SyncTeamMemberCountListener.ts
@modular-monolith/src/utils/event-bus/idempotency.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TeamService Interface and Implementation</name>
  <files>modular-monolith/src/modules/team/TeamService.ts, modular-monolith/src/modules/team/internal/TeamServiceImpl.ts</files>
  <action>
    Implement the following methods in `TeamService` and `TeamServiceImpl`:
    - `handleSyncTeamMemberCount(events)`
    - `handleRemoveProjectTeamMember(events)`
    - `handleDeleteProjectTeamMember(events)`
    - `handleDeleteProjectTeam(events)`
    - `handlePurgeTeamMemberships(events)`
    - `handleSyncTeamTaskCount(events)`
    
    Each method must:
    1. Start a `db.transaction()`.
    2. Call `claimEventsAtomic(trx, events, groupId)` with the appropriate groupId for the listener.
    3. Consolidate data/events if necessary.
    4. Perform bulk updates/deletions using existing Query functions.
  </action>
  <verify>
    <automated>grep -E "handleSyncTeamMemberCount|handleRemoveProjectTeamMember|handleDeleteProjectTeamMember|handleDeleteProjectTeam|handlePurgeTeamMemberships|handleSyncTeamTaskCount" modular-monolith/src/modules/team/internal/TeamServiceImpl.ts</automated>
  </verify>
  <done>TeamService updated with required batch methods.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TeamService Batch Sync Tests</name>
  <files>modular-monolith/src/modules/team/internal/__tests__/TeamServiceBatch.test.ts</files>
  <behavior>
    - Test 1: handleSyncTeamMemberCount correctly consolidates deltas per team.
    - Test 2: handlePurgeTeamMemberships executes bulk deletion for provided team IDs.
    - Test 3: handleDeleteProjectTeam executes bulk deletion for provided project IDs.
    - Test 4: All methods correctly call claimEventsAtomic and wrap operations in a transaction.
  </behavior>
  <action>
    Create unit tests in `TeamServiceBatch.test.ts`.
    - Mock `db`, `claimEventsAtomic`, and underlying domain query functions.
    - Verify data consolidation and transaction handling for every new service method.
  </action>
  <verify>
    <automated>npm test modular-monolith/src/modules/team/internal/__tests__/TeamServiceBatch.test.ts</automated>
  </verify>
  <done>All TeamService batch methods are fully tested.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Team Module Listeners</name>
  <files>
    modular-monolith/src/modules/team/internal/listeners/TeamAggregated_SyncTeamMemberCountListener.ts,
    modular-monolith/src/modules/team/internal/listeners/ProjectAggregated_RemoveProjectTeamMember.ts,
    modular-monolith/src/modules/team/internal/listeners/ProjectAggregated_DeleteProjectTeamMember.ts,
    modular-monolith/src/modules/team/internal/listeners/ProjectAggregated_DeleteProjectTeam.ts,
    modular-monolith/src/modules/team/internal/listeners/TeamAggregated_PurgeTeamMembershipsListener.ts,
    modular-monolith/src/modules/team/internal/listeners/TaskAggregated_SyncTeamTaskCountListener.ts
  </files>
  <action>
    Refactor identified team listeners to delegate to `teamService`.
    - Inject/use `teamService`.
    - Replace transaction/idempotency logic with service method calls.
    - Remove `import { db }` from each listener.
  </action>
  <verify>
    <automated>grep -v "import.*db" modular-monolith/src/modules/team/internal/listeners/*.ts | grep "teamService."</automated>
  </verify>
  <done>All team aggregated listeners refactored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka -> Team Listeners | Aggregated signals from Project, Task, and Team aggregators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Tampering | Team/Member Tables | mitigate | TeamService handles all transactions and idempotency checks. |
</threat_model>

<verification>
Ensure member counts and task counts on Teams update correctly during batch processing. Verify new TeamService batch tests pass.
</verification>

<success_criteria>
Team module listeners have zero direct `db` references and delegate to TeamService. New service methods are verified by unit tests.
</success_criteria>

<output>
Create `.planning/phases/39-smart-aggregator-isolation/39-05-SUMMARY.md` when done
</output>
