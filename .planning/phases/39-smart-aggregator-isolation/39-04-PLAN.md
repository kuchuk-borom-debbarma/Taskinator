---
phase: 39-smart-aggregator-isolation
plan: 04
type: execute
wave: 2
depends_on: ["39-01"]
files_modified: [
    "modular-monolith/src/modules/project/ProjectService.ts",
    "modular-monolith/src/modules/project/internal/ProjectServiceImpl.ts",
    "modular-monolith/src/modules/project/internal/listeners/ProjectAggregated_ChangeProjectMemberCount.ts",
    "modular-monolith/src/modules/project/internal/listeners/TaskAggregated_SyncProjectTaskCountListener.ts",
    "modular-monolith/src/modules/project/internal/listeners/TeamAggregated_SyncProjectTeamCountListener.ts",
    "modular-monolith/src/modules/project/internal/listeners/ProjectAggregated_RemoveProjectMember.ts",
    "modular-monolith/src/modules/project/internal/listeners/ProjectAggregated_DeleteProjectMember.ts",
    "modular-monolith/src/modules/project/internal/__tests__/ProjectServiceBatch.test.ts"
]
autonomous: true
requirements: [R3, R4]
must_haves:
  truths:
    - "ProjectService handles aggregated member/task/team count synchronization"
    - "Project listeners decoupled from direct DB transaction management"
    - "All ProjectService batch methods are verified by unit tests"
  artifacts:
    - path: "modular-monolith/src/modules/project/ProjectService.ts"
      contains: ["handleProjectMemberCountSync", "handleSyncProjectTaskCount", "handleSyncProjectTeamCount", "handleRemoveProjectMember", "handleDeleteProjectMember"]
    - path: "modular-monolith/src/modules/project/internal/listeners/ProjectAggregated_ChangeProjectMemberCount.ts"
      contains: "projectService.handleProjectMemberCountSync"
    - path: "modular-monolith/src/modules/project/internal/__tests__/ProjectServiceBatch.test.ts"
      provides: "Unit tests for ProjectService batch processing"
---

<objective>
Update ProjectService with bulk processing methods, refactor project module aggregated listeners, and verify the implementation with comprehensive tests.

Purpose: Enforce service layer isolation for Project module with guaranteed correctness.
Output: Extended ProjectService, refactored project listeners, and verification tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/39-RESEARCH.md
@modular-monolith/src/modules/project/ProjectService.ts
@modular-monolith/src/modules/project/internal/ProjectServiceImpl.ts
@modular-monolith/src/modules/project/internal/listeners/ProjectAggregated_ChangeProjectMemberCount.ts
@modular-monolith/src/utils/event-bus/idempotency.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ProjectService Interface and Implementation</name>
  <files>modular-monolith/src/modules/project/ProjectService.ts, modular-monolith/src/modules/project/internal/ProjectServiceImpl.ts</files>
  <action>
    Implement the following methods in `ProjectService` and `ProjectServiceImpl`:
    - `handleProjectMemberCountSync(events)`
    - `handleRemoveProjectMember(events)`
    - `handleDeleteProjectMember(events)`
    - `handleSyncProjectTaskCount(events)`
    - `handleSyncProjectTeamCount(events)`
    
    Each method must:
    1. Start a `db.transaction()`.
    2. Call `claimEventsAtomic(trx, events, groupId)` with the appropriate groupId for the listener.
    3. Consolidate data/events if necessary.
    4. Perform bulk updates/deletions using existing Query functions.
  </action>
  <verify>
    <automated>grep -E "handleProjectMemberCountSync|handleRemoveProjectMember|handleDeleteProjectMember|handleSyncProjectTaskCount|handleSyncProjectTeamCount" modular-monolith/src/modules/project/internal/ProjectServiceImpl.ts</automated>
  </verify>
  <done>ProjectService updated with required batch methods.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ProjectService Batch Sync Tests</name>
  <files>modular-monolith/src/modules/project/internal/__tests__/ProjectServiceBatch.test.ts</files>
  <behavior>
    - Test 1: handleProjectMemberCountSync correctly consolidates deltas per project.
    - Test 2: handleRemoveProjectMember executes bulk deletion for provided user IDs.
    - Test 3: handleDeleteProjectMember executes bulk deletion for provided project IDs.
    - Test 4: All methods correctly call claimEventsAtomic and wrap operations in a transaction.
  </behavior>
  <action>
    Create unit tests in `ProjectServiceBatch.test.ts`.
    - Mock `db`, `claimEventsAtomic`, and underlying domain query functions.
    - Verify data consolidation and transaction handling for every new service method.
  </action>
  <verify>
    <automated>npm test modular-monolith/src/modules/project/internal/__tests__/ProjectServiceBatch.test.ts</automated>
  </verify>
  <done>All ProjectService batch methods are fully tested.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Project Module Listeners</name>
  <files>
    modular-monolith/src/modules/project/internal/listeners/ProjectAggregated_ChangeProjectMemberCount.ts,
    modular-monolith/src/modules/project/internal/listeners/TaskAggregated_SyncProjectTaskCountListener.ts,
    modular-monolith/src/modules/project/internal/listeners/TeamAggregated_SyncProjectTeamCountListener.ts,
    modular-monolith/src/modules/project/internal/listeners/ProjectAggregated_RemoveProjectMember.ts,
    modular-monolith/src/modules/project/internal/listeners/ProjectAggregated_DeleteProjectMember.ts
  </files>
  <action>
    Refactor identified project listeners to delegate to `projectService`.
    - Inject/use `projectService`.
    - Replace transaction/idempotency logic with service method calls.
    - Remove `import { db }` from each listener.
  </action>
  <verify>
    <automated>grep -v "import.*db" modular-monolith/src/modules/project/internal/listeners/*.ts | grep "projectService."</automated>
  </verify>
  <done>All project aggregated listeners refactored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka -> Project Listeners | Aggregated signals from Project, Task, and Team aggregators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05 | Tampering | Project/Member Tables | mitigate | ProjectService handles all transactions and idempotency checks. |
</threat_model>

<verification>
Ensure member counts, task counts, and team counts on Projects update correctly during batch processing. Verify new ProjectService batch tests pass.
</verification>

<success_criteria>
Project module listeners have zero direct `db` references and delegate to ProjectService. New service methods are verified by unit tests.
</success_criteria>

<output>
Create `.planning/phases/39-smart-aggregator-isolation/39-04-SUMMARY.md` when done
</output>
