---
phase: 39-smart-aggregator-isolation
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified: [
    "modular-monolith/src/kafka/smart-aggregator-consumer/task/TaskEvents_BatchAggregator.ts",
    "modular-monolith/src/kafka/smart-aggregator-consumer/team/TeamEvents_BatchAggregator.ts"
]
autonomous: true
requirements: [R1, R4]
must_haves:
  truths:
    - "Task aggregator removed from direct DB transaction management"
    - "Team aggregator removed from direct DB transaction management"
  artifacts:
    - path: "modular-monolith/src/kafka/smart-aggregator-consumer/task/TaskEvents_BatchAggregator.ts"
      contains: "aggregatorService.processAggregatorBatch"
    - path: "modular-monolith/src/kafka/smart-aggregator-consumer/team/TeamEvents_BatchAggregator.ts"
      contains: "aggregatorService.processAggregatorBatch"
---

<objective>
Refactor Task and Team aggregators to use the new `AggregatorService`.

Purpose: Complete the decoupling of all smart aggregators from direct database access.
Output: Refactored task and team aggregators.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/39-RESEARCH.md
@modular-monolith/src/kafka/smart-aggregator-consumer/task/TaskEvents_BatchAggregator.ts
@modular-monolith/src/kafka/smart-aggregator-consumer/team/TeamEvents_BatchAggregator.ts
@modular-monolith/src/utils/event-bus/AggregatorService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor TaskEvents_BatchAggregator</name>
  <files>modular-monolith/src/kafka/smart-aggregator-consumer/task/TaskEvents_BatchAggregator.ts</files>
  <action>
    Refactor `TaskEvents_BatchAggregator` to use `aggregatorService.processAggregatorBatch`.
    - Use `AggregatorService` to handle batch processing.
    - Move folding logic into the `folder` callback.
    - Remove `import { db }` and direct transaction handling.
  </action>
  <verify>
    <automated>grep -v "import.*db" modular-monolith/src/kafka/smart-aggregator-consumer/task/TaskEvents_BatchAggregator.ts | grep "aggregatorService.processAggregatorBatch"</automated>
  </verify>
  <done>Task aggregator refactored.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor TeamEvents_BatchAggregator</name>
  <files>modular-monolith/src/kafka/smart-aggregator-consumer/team/TeamEvents_BatchAggregator.ts</files>
  <action>
    Refactor `TeamEvents_BatchAggregator` to use `aggregatorService.processAggregatorBatch`.
    - Use `AggregatorService` to handle batch processing.
    - Move folding logic into the `folder` callback.
    - Remove `import { db }` and direct transaction handling.
  </action>
  <verify>
    <automated>grep -v "import.*db" modular-monolith/src/kafka/smart-aggregator-consumer/team/TeamEvents_BatchAggregator.ts | grep "aggregatorService.processAggregatorBatch"</automated>
  </verify>
  <done>Team aggregator refactored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka -> Aggregator | Untrusted events from broker are processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Tampering | Outbox Entry | mitigate | AggregatorService uses database transactions to ensure atomic outbox writes. |
</threat_model>

<verification>
Verify no direct `db` imports remain in the `smart-aggregator-consumer/` directory.
</verification>

<success_criteria>
All smart aggregators use AggregatorService and have zero direct `db` references.
</success_criteria>

<output>
Create `.planning/phases/39-smart-aggregator-isolation/39-02-SUMMARY.md` when done
</output>
