---
phase: 39-smart-aggregator-isolation
plan: 06
type: execute
wave: 2
depends_on: ["39-01"]
files_modified: [
    "modular-monolith/src/modules/task/TaskService.ts",
    "modular-monolith/src/modules/task/internal/TaskServiceImpl.ts",
    "modular-monolith/src/modules/task/internal/listeners/TaskAggregated_ReachabilitySyncListener.ts",
    "modular-monolith/src/modules/task/internal/listeners/TaskAggregated_DeleteTaskLinksListener.ts",
    "modular-monolith/src/modules/task/internal/listeners/TaskAggregated_DeleteTaskReachabilityListener.ts",
    "modular-monolith/src/modules/task/internal/__tests__/TaskServiceGraph.test.ts"
]
autonomous: true
requirements: [R3, R4]
must_haves:
  truths:
    - "TaskService handles reachability and link synchronization"
    - "Idempotency bug in ReachabilitySyncListener is fixed"
    - "TaskService graph methods are verified by unit tests"
  artifacts:
    - path: "modular-monolith/src/modules/task/TaskService.ts"
      contains: ["handleTaskReachabilitySync", "handleDeleteTaskLinks", "handleDeleteTaskReachability"]
    - path: "modular-monolith/src/modules/task/internal/listeners/TaskAggregated_ReachabilitySyncListener.ts"
      contains: "taskService.handleTaskReachabilitySync"
    - path: "modular-monolith/src/modules/task/internal/__tests__/TaskServiceGraph.test.ts"
      provides: "Unit tests for TaskService graph processing"
---

<objective>
Update TaskService with reachability and link processing methods, fix the idempotency bug in TaskAggregated_ReachabilitySyncListener, and verify with tests.

Purpose: Enforce service layer isolation for Task module graph operations with guaranteed correctness.
Output: Extended TaskService, refactored task graph listeners, and verification tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/39-RESEARCH.md
@modular-monolith/src/modules/task/TaskService.ts
@modular-monolith/src/modules/task/internal/TaskServiceImpl.ts
@modular-monolith/src/modules/task/internal/listeners/TaskAggregated_ReachabilitySyncListener.ts
@modular-monolith/src/utils/event-bus/idempotency.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TaskService Interface and Implementation</name>
  <files>modular-monolith/src/modules/task/TaskService.ts, modular-monolith/src/modules/task/internal/TaskServiceImpl.ts</files>
  <action>
    Implement the following methods in `TaskService` and `TaskServiceImpl`:
    - `handleTaskReachabilitySync(events)` (MANDATORY: MUST include `claimEventsAtomic` to fix the reported bug)
    - `handleDeleteTaskLinks(events)`
    - `handleDeleteTaskReachability(events)`
    
    Each method must:
    1. Start a `db.transaction()`.
    2. Call `claimEventsAtomic(trx, events, groupId)`.
    3. Perform bulk updates/deletions using existing Query functions (e.g., `expandTaskReachability`, `contractTaskReachability`).
  </action>
  <verify>
    <automated>grep -E "handleTaskReachabilitySync|handleDeleteTaskLinks|handleDeleteTaskReachability" modular-monolith/src/modules/task/internal/TaskServiceImpl.ts && grep "claimEventsAtomic" modular-monolith/src/modules/task/internal/TaskServiceImpl.ts</automated>
  </verify>
  <done>TaskService updated with graph sync methods and idempotency fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TaskService Graph Sync Tests</name>
  <files>modular-monolith/src/modules/task/internal/__tests__/TaskServiceGraph.test.ts</files>
  <behavior>
    - Test 1: handleTaskReachabilitySync calls claimEventsAtomic (fixing the idempotency bug).
    - Test 2: handleDeleteTaskLinks executes bulk deletion for provided task IDs.
    - Test 3: handleDeleteTaskReachability executes bulk deletion for provided task IDs.
    - Test 4: All methods correctly wrap operations in a transaction.
  </behavior>
  <action>
    Create unit tests in `TaskServiceGraph.test.ts`.
    - Mock `db`, `claimEventsAtomic`, and graph query functions.
    - Verify idempotency check exists for reachability sync.
    - Verify transaction handling for graph operations.
  </action>
  <verify>
    <automated>npm test modular-monolith/src/modules/task/internal/__tests__/TaskServiceGraph.test.ts</automated>
  </verify>
  <done>TaskService graph methods are fully tested.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Task Graph Listeners</name>
  <files>
    modular-monolith/src/modules/task/internal/listeners/TaskAggregated_ReachabilitySyncListener.ts,
    modular-monolith/src/modules/task/internal/listeners/TaskAggregated_DeleteTaskLinksListener.ts,
    modular-monolith/src/modules/task/internal/listeners/TaskAggregated_DeleteTaskReachabilityListener.ts
  </files>
  <action>
    Refactor identified task graph listeners to delegate to `taskService`.
    - Inject/use `taskService`.
    - Replace internal logic with service calls.
    - Remove `import { db }` and direct query calls.
  </action>
  <verify>
    <automated>grep -v "import.*db" modular-monolith/src/modules/task/internal/listeners/TaskAggregated_ReachabilitySyncListener.ts | grep "taskService.handleTaskReachabilitySync"</automated>
  </verify>
  <done>Task graph listeners refactored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka -> Task Listeners | Aggregated signals from Task aggregator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07 | Tampering | Task Tables | mitigate | TaskService handles all transactions and idempotency checks. |
</threat_model>

<verification>
Run task graph integration tests to ensure transitive closure expansion and contraction still work correctly. Verify new TaskService graph tests pass.
</verification>

<success_criteria>
Reachability listener has zero direct `db` references and includes idempotency protection. New service methods are verified by unit tests.
</success_criteria>

<output>
Create `.planning/phases/39-smart-aggregator-isolation/39-06-SUMMARY.md` when done
</output>
