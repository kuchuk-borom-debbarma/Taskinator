---
phase: 39-smart-aggregator-isolation
plan: 07
type: execute
wave: 3
depends_on: ["39-06"]
files_modified: [
    "modular-monolith/src/modules/task/TaskService.ts",
    "modular-monolith/src/modules/task/internal/TaskServiceImpl.ts",
    "modular-monolith/src/modules/task/internal/listeners/ProjectAggregated_UnassignProjectTaskMember.ts",
    "modular-monolith/src/modules/task/internal/listeners/ProjectAggregated_DeleteProjectTask.ts",
    "modular-monolith/src/modules/task/internal/listeners/ProjectAggregated_DeleteProjectTaskLink.ts",
    "modular-monolith/src/modules/task/internal/listeners/ProjectAggregated_DeleteProjectReachability.ts",
    "modular-monolith/src/modules/task/internal/listeners/TeamAggregated_OrphanTeamTasksListener.ts",
    "modular-monolith/src/modules/task/internal/listeners/TeamAggregated_UnassignMemberFromTeamTasksListener.ts",
    "modular-monolith/src/modules/task/internal/__tests__/TaskServiceCleanup.test.ts"
]
autonomous: true
requirements: [R3, R4]
must_haves:
  truths:
    - "TaskService handles member unassignment and project cleanup"
    - "Task listeners decoupled from direct DB transaction management"
    - "TaskService cleanup methods are verified by unit tests"
  artifacts:
    - path: "modular-monolith/src/modules/task/TaskService.ts"
      contains: ["handleUnassignProjectTaskMember", "handleDeleteProjectTask", "handleDeleteProjectTaskLink", "handleDeleteProjectReachability", "handleOrphanTeamTasks", "handleUnassignMemberFromTeamTasks"]
    - path: "modular-monolith/src/modules/task/internal/listeners/ProjectAggregated_UnassignProjectTaskMember.ts"
      contains: "taskService.handleUnassignProjectTaskMember"
    - path: "modular-monolith/src/modules/task/internal/__tests__/TaskServiceCleanup.test.ts"
      provides: "Unit tests for TaskService cleanup and assignment processing"
---

<objective>
Refactor remaining Task module aggregated listeners, extend TaskService to support assignments and bulk deletions, and verify with tests.

Purpose: Complete service layer isolation for Task module with guaranteed correctness.
Output: Fully extended TaskService, refactored task listeners, and verification tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/39-RESEARCH.md
@modular-monolith/src/modules/task/TaskService.ts
@modular-monolith/src/modules/task/internal/TaskServiceImpl.ts
@modular-monolith/src/modules/task/internal/listeners/ProjectAggregated_UnassignProjectTaskMember.ts
@modular-monolith/src/utils/event-bus/idempotency.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TaskService Interface and Implementation (Remaining Methods)</name>
  <files>modular-monolith/src/modules/task/TaskService.ts, modular-monolith/src/modules/task/internal/TaskServiceImpl.ts</files>
  <action>
    Implement the following methods in `TaskService` and `TaskServiceImpl`:
    - `handleUnassignProjectTaskMember(events)`
    - `handleDeleteProjectTask(events)`
    - `handleDeleteProjectTaskLink(events)`
    - `handleDeleteProjectReachability(events)`
    - `handleOrphanTeamTasks(events)`
    - `handleUnassignMemberFromTeamTasks(events)`
    
    Each method must:
    1. Start a `db.transaction()`.
    2. Call `claimEventsAtomic(trx, events, groupId)`.
    3. Perform bulk updates/deletions using existing Query functions.
  </action>
  <verify>
    <automated>grep -E "handleUnassignProjectTaskMember|handleDeleteProjectTask|handleDeleteProjectTaskLink|handleDeleteProjectReachability|handleOrphanTeamTasks|handleUnassignMemberFromTeamTasks" modular-monolith/src/modules/task/internal/TaskServiceImpl.ts</automated>
  </verify>
  <done>TaskService fully updated with cleanup and assignment methods.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TaskService Cleanup Tests</name>
  <files>modular-monolith/src/modules/task/internal/__tests__/TaskServiceCleanup.test.ts</files>
  <behavior>
    - Test 1: handleUnassignProjectTaskMember executes bulk update to nullify member IDs.
    - Test 2: handleDeleteProjectTask executes bulk deletion for provided project IDs.
    - Test 3: handleOrphanTeamTasks nullifies team IDs for tasks in provided teams.
    - Test 4: All methods correctly call claimEventsAtomic and wrap operations in a transaction.
  </behavior>
  <action>
    Create unit tests in `TaskServiceCleanup.test.ts`.
    - Mock `db`, `claimEventsAtomic`, and underlying domain query functions.
    - Verify data consolidation and transaction handling for every new cleanup method.
  </action>
  <verify>
    <automated>npm test modular-monolith/src/modules/task/internal/__tests__/TaskServiceCleanup.test.ts</automated>
  </verify>
  <done>All TaskService cleanup methods are fully tested.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Remaining Task Module Listeners</name>
  <files>
    modular-monolith/src/modules/task/internal/listeners/ProjectAggregated_UnassignProjectTaskMember.ts,
    modular-monolith/src/modules/task/internal/listeners/ProjectAggregated_DeleteProjectTask.ts,
    modular-monolith/src/modules/task/internal/listeners/ProjectAggregated_DeleteProjectTaskLink.ts,
    modular-monolith/src/modules/task/internal/listeners/ProjectAggregated_DeleteProjectReachability.ts,
    modular-monolith/src/modules/task/internal/listeners/TeamAggregated_OrphanTeamTasksListener.ts,
    modular-monolith/src/modules/task/internal/listeners/TeamAggregated_UnassignMemberFromTeamTasksListener.ts
  </files>
  <action>
    Refactor remaining task listeners to delegate to `taskService`.
    - Inject/use `taskService`.
    - Replace internal logic with service calls.
    - Remove `import { db }` and direct query calls.
  </action>
  <verify>
    <automated>grep -v "import.*db" modular-monolith/src/modules/task/internal/listeners/*.ts | grep "taskService."</automated>
  </verify>
  <done>All remaining task listeners refactored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka -> Task Listeners | Aggregated signals from Project and Team aggregators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08 | Tampering | Task Tables | mitigate | TaskService handles all transactions and idempotency checks. |
</threat_model>

<verification>
Ensure cascading deletions (Project -> Task) and team-task unassignments work correctly. Verify new TaskService cleanup tests pass.
</verification>

<success_criteria>
All task module aggregated listeners have zero direct `db` references and delegate to TaskService. New service methods are verified by unit tests.
</success_criteria>

<output>
Create `.planning/phases/39-smart-aggregator-isolation/39-07-SUMMARY.md` when done
</output>
