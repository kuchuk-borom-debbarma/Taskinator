---
phase: 15-refactoring-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [remotion/src/FeatureShowcase.tsx]
autonomous: true
requirements: [REFACTOR-01]

must_haves:
  truths:
    - "All Sequence components in FeatureShowcase.tsx define explicit premountFor property"
    - "All interpolate calls mapping numerical timing variables define explicit clamp and easing properties"
  artifacts:
    - path: "remotion/src/FeatureShowcase.tsx"
      provides: "FeatureShowcase composition"
      contains: "premountFor"
---

<objective>
Refactor FeatureShowcase.tsx to conform perfectly to Remotion Best Practice guidelines.

Purpose: Ensure rock-solid rendering stability and smooth, deterministic animations without relying on default, unclamped linear mappings.
Output: Fully optimized FeatureShowcase.tsx.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-refactoring-baseline/15-RESEARCH.md
@remotion/src/FeatureShowcase.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit premountFor and layout properties to all Sequences in FeatureShowcase.tsx</name>
  <files>remotion/src/FeatureShowcase.tsx</files>
  <read_first>remotion/src/FeatureShowcase.tsx, .planning/phases/15-refactoring-baseline/15-RESEARCH.md</read_first>
  <action>Inspect all <Sequence> declarations in FeatureShowcase.tsx. Add `premountFor={1 * fps}` to every single instance to prevent flickering during rendering. For the sub-sequences that do not require direct absolute wrapping (like the TitleCard sequence and TypographyIntro sequence), set `layout="none"`. Apply this to the sequences at lines 93, 98, 103, 128, 144, and 159.</action>
  <verify>grep -c "premountFor" remotion/src/FeatureShowcase.tsx</verify>
  <acceptance_criteria>
    - FeatureShowcase.tsx contains at least 6 occurrences of "premountFor"
  </acceptance_criteria>
  <done>All sequences are safely pre-buffered and laid out.</done>
</task>

<task type="auto">
  <name>Task 2: Inject explicit clamping and Easing curves to all interpolate calls in FeatureShowcase.tsx</name>
  <files>remotion/src/FeatureShowcase.tsx</files>
  <read_first>remotion/src/FeatureShowcase.tsx, .planning/phases/15-refactoring-baseline/15-RESEARCH.md</read_first>
  <action>Locate the standard frame-based interpolations in FeatureShowcase.tsx. Specifically, refactor the `interpolate` calls on lines 25, 26, 122, and 145. Pass an options object `{ extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: Easing.out(Easing.cubic) }` (or Easing.bezier(0.16, 1, 0.3, 1)) where appropriate. Import `Easing` from "remotion" at the top of the file.</action>
  <verify>grep "extrapolateRight" remotion/src/FeatureShowcase.tsx</verify>
  <acceptance_criteria>
    - FeatureShowcase.tsx imports "Easing" from "remotion"
    - FeatureShowcase.tsx contains "extrapolateRight" in interpolate calls
  </acceptance_criteria>
  <done>All interpolations are safely clamped and eased.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify file compiles cleanly via `npx tsc --noEmit` inside the remotion root directory if available.
- [ ] Review code changes to confirm zero regressions in logical sequencing.
</verification>

<success_criteria>
- All tasks completed
- FeatureShowcase adheres completely to the best practice sequencing and interpolation rules
</success_criteria>

<output>
After completion, create `.planning/phases/15-refactoring-baseline/15-01-SUMMARY.md`
</output>
