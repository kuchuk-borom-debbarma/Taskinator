---
phase: 15-refactoring-baseline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - remotion/src/SyncArchitecture.tsx
  - remotion/src/BlockingProblem.tsx
  - remotion/src/AsyncSolution.tsx
autonomous: true
requirements: [REFACTOR-03]

must_haves:
  truths:
    - "SyncArchitecture.tsx integrates premountFor={1 * fps} on all Sequence components"
    - "All interpolate functions in BlockingProblem and AsyncSolution declare clamp configurations"
  artifacts:
    - path: "remotion/src/SyncArchitecture.tsx"
      provides: "Master sequence controller for evolutionary flow"
    - path: "remotion/src/BlockingProblem.tsx"
      provides: "Thread blocking visual sequence"
---

<objective>
Refactor SyncArchitecture composition and its blocking/async solution slides to conform perfectly to Remotion timing specifications.

Purpose: Ensure complex multi-node event traces build consistently and that frame bound overflows are completely suppressed.
Output: Three optimized React/Remotion source files.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-refactoring-baseline/15-RESEARCH.md
@remotion/src/SyncArchitecture.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor SyncArchitecture.tsx sequences and interpolation clamping</name>
  <files>remotion/src/SyncArchitecture.tsx</files>
  <read_first>remotion/src/SyncArchitecture.tsx, .planning/phases/15-refactoring-baseline/15-RESEARCH.md</read_first>
  <action>1. Import `Easing` from "remotion" at the top of SyncArchitecture.tsx.
2. Visit all Sequence blocks in SyncArchitecture.tsx, including those in the root `SyncArchitecture` component (lines 315-347) and inside nested subcomponents like `CreateTask`, `CreateTaskLink`, and `ReadProject`. Add `premountFor={1 * fps}` and `layout="none"` to every sequence.
3. Add explicit interpolation clamping to all calls of `interpolate` inside `Arrow`, `DbStep`, `Banner`, `Shell`, `ReadProject`, and `RoadmapSlide` (e.g. `{ extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: Easing.bezier(0.16, 1, 0.3, 1) }`).</action>
  <verify>grep "premountFor" remotion/src/SyncArchitecture.tsx</verify>
  <acceptance_criteria>
    - SyncArchitecture.tsx contains "premountFor" on sequences
    - SyncArchitecture.tsx has "extrapolateLeft" inside its interpolate calls
  </acceptance_criteria>
  <done>SyncArchitecture.tsx sequences and curves completely optimized.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor BlockingProblem.tsx and AsyncSolution.tsx to follow clamping and Easing guidelines</name>
  <files>remotion/src/BlockingProblem.tsx, remotion/src/AsyncSolution.tsx</files>
  <read_first>remotion/src/BlockingProblem.tsx, remotion/src/AsyncSolution.tsx</read_first>
  <action>1. Import `Easing` from "remotion" in both files.
2. Search for all instances of `interpolate(...)` inside both files.
3. Add the options object `{ extrapolateLeft: "clamp", extrapolateRight: "clamp", easing: Easing.bezier(0.16, 1, 0.3, 1) }` (or `Easing.out(Easing.cubic)`) to every call.
4. Add `premountFor={1 * fps}` and `layout="none"` to any Sequence wrappers defined in these subcomponents.</action>
  <verify>grep -l "extrapolateLeft" remotion/src/BlockingProblem.tsx remotion/src/AsyncSolution.tsx</verify>
  <acceptance_criteria>
    - Both files successfully include "extrapolateLeft"
  </acceptance_criteria>
  <done>Blocking problem and Async solution files migrated to clamped ease curves.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify codebase compiles without errors.
- [ ] Verify master composition timeline executes smoothly.
</verification>

<success_criteria>
- All tasks completed
- Complete evolutionary flow (Act 2) adheres to proper Remotion API limits.
</success_criteria>

<output>
After completion, create `.planning/phases/15-refactoring-baseline/15-03-SUMMARY.md`
</output>
