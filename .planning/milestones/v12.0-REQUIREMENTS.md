# Requirements: Milestone v12.0 (Tied Rule Orchestration) — ARCHIVED

**Shipped:** 2026-05-21
**Status:** ✅ All requirements met

---

## 1. Type-Safe Sequential Flow Schema

* [x] **REQ-01**: `ActionStep` (`{ type: 'action', actionId, inputs }`) and `ConditionActionStep` (`{ type: 'condition_action', condition, actionId, inputs }`) — Implemented in `auto-action-engine/types.ts`
* [x] **REQ-02**: Combined `pipelineStepSchema` discriminated union — Implemented
* [x] **REQ-03**: `autoActionFlowSchema` as array of `PipelineStep` — Implemented

---

## 2. DB Schema & CRUD Manager with Constraints

* [x] **REQ-04**: `AutoAction` Kysely table mapping — `AutoActionTable` in `src/database/tables/AutoAction.ts`; `auto_action` table in `schema.sql`
* [x] **REQ-05**: Project-scoped unique name validation — `createAutoAction` / `updateAutoAction` enforce active-name uniqueness per `fk_project_id`
* [x] **REQ-06**: CRUD: `createAutoAction`, `updateAutoAction`, `deleteAutoAction`, `getAutoActionsForProject` — All in `manager.ts` with OCC

---

## 3. Sync/Async Boundaries & Dynamic Template Catalog

* [x] **REQ-07**: `is_sync` boolean on `AutoAction` — Column in DB schema and type definition
* [x] **REQ-08**: Sync flow validation — `isFlowSyncSafe(steps)` + `isConditionAsync(node)` reject async steps in sync flows
* [x] **REQ-09**: `getTemplateForScope(scope, isSync)` filters async definitions — Implemented in `template.ts`

---

## 4. Sequential Flow Executor

* [x] **REQ-10**: `executeAutoActionPipeline` — sequential context fetch → condition eval → action exec per step; supports `startIndex` + `startCursor` for suspend/resume

> **Outcome note:** REQ-10 originally scoped a simple `executeAutoAction(autoActionId, contextSnapshot)`. Delivered as `executeAutoActionPipeline(autoActionId, entityId, actorId, traceId, wasSnapshot, startIndex, startCursor)` with suspendable execution and condition splitting — a superset of the requirement.

---

## 5. Comprehensive Testing

* [x] **REQ-11**: 48 tests across 3 files — sequential execution, condition paths (true/false), sync boundary rejection, name uniqueness, template filtering, condition splitting (4 tests), pipeline cursor resume (1 test)

---

## Beyond-Scope Hardening (Shipped in v12.0)

* Persistent `prev_` columns in `project_task` — atomic writes across 7 query paths; DB-first priority in context resolver
* `StepResumeCursor` / `evaluateConditionFromIndex` — condition splitting architecture for future async chunking
* Legacy `autopilot` module teardown — 63 source files removed
* Docs: new `orchestration.md`, updated `context.md`, updated `README.md`

---

## Deferred to Future Milestones

| Item | Target |
|---|---|
| GraphQL resolvers for `auto_action` CRUD | v13.0+ |
| Trigger listener wiring (event bus → pipeline) | v13.0+ |
| Condition split state persistence (`conditionChildIndex` in DB) | v13.0+ |
| Async pipeline execution queue | v13.0+ |
