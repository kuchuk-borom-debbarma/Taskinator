# Requirements: v14.0 — Hide Internal Engines

**Status: ✅ SHIPPED 2026-05-21**

---

## 1. Internal Folder Structure

* [x] **REQ-01** — Create functional sub-directories in `src/modules/auto-action/internal/`: `engines/`, `execution/`, `service/`, `queries/`.
* [x] **REQ-02** — Move `AutoActionServiceImpl.ts` to `internal/service/`.
* [x] **REQ-03** — Move `AutoActionQueries.ts` to `internal/queries/`.

## 2. Engine Hiding & Consolidation

* [x] **REQ-04** — Move `actionEngine.ts`, `conditionEngine.ts`, and `contextEngine.ts` to `internal/engines/`.
* [x] **REQ-05** — Move `executor.ts` and `template.ts` from `auto-action-engine/` to `internal/execution/`.
* [x] **REQ-06** — Consolidate types from `auto-action-engine/types.ts` into top-level `types.ts`.
* [x] **REQ-07** — Delete the `auto-action-engine/` directory once empty.

## 3. Import & Path Updates

* [x] **REQ-08** — Update all internal module imports to reflect the new directory structure.
* [x] **REQ-09** — Ensure `src/modules/auto-action/index.ts` remains the single entry point for the module, exporting only the public service and types.

## 4. Verification

* [x] **REQ-10** — All 48 existing tests pass after restructuring. No regression in functionality.
* [x] **REQ-11** — Zero TypeScript compilation errors in the `auto-action` module.
