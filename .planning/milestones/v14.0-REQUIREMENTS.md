# Requirements: v14.0 — Hide Internal Engines

**Status: ⏳ PLANNED**

---

## 1. Internal Folder Structure

* [ ] **REQ-01** — Create functional sub-directories in `src/modules/auto-action/internal/`: `engines/`, `execution/`, `service/`, `queries/`.
* [ ] **REQ-02** — Move `AutoActionServiceImpl.ts` to `internal/service/`.
* [ ] **REQ-03** — Move `AutoActionQueries.ts` to `internal/queries/`.

## 2. Engine Hiding & Consolidation

* [ ] **REQ-04** — Move `actionEngine.ts`, `conditionEngine.ts`, and `contextEngine.ts` to `internal/engines/`.
* [ ] **REQ-05** — Move `executor.ts` and `template.ts` from `auto-action-engine/` to `internal/execution/`.
* [ ] **REQ-06** — Consolidate types from `auto-action-engine/types.ts` into top-level `types.ts`.
* [ ] **REQ-07** — Delete the `auto-action-engine/` directory once empty.

## 3. Import & Path Updates

* [ ] **REQ-08** — Update all internal module imports to reflect the new directory structure.
* [ ] **REQ-09** — Ensure `src/modules/auto-action/index.ts` remains the single entry point for the module, exporting only the public service and types.

## 4. Verification

* [ ] **REQ-10** — All 48 existing tests pass after restructuring. No regression in functionality.
* [ ] **REQ-11** — Zero TypeScript compilation errors in the `auto-action` module.
